# Verification Report — stage-15-cc-proveedores-ledger

**Change**: Stage 15 — Cuenta corriente de proveedores (ledger)
**HEAD verified**: `8c03226` (6 slices merged: PRs #134-#139, stacked-to-main)
**Date**: 2026-08-18
**Verdict: PASS WITH WARNINGS** (0 CRITICAL, 2 WARNING, 2 SUGGESTION, all remediable pre-archive)

## Method note

The orchestrator's full suites (Domain 492/492, Application 286/286, Integration 1297/1297 in
isolation, vitest 730/730, 2026-08-18) were accepted as evidence and not re-run in full. Verify
ran its own filtered spot-checks plus source inspection against every binding artifact:

| Check | Result |
|---|---|
| `dotnet ef migrations has-pending-model-changes` | clean - "No changes have been made to the model since the last migration." |
| `dotnet test --filter FullyQualifiedName~CuentaCorrienteProveedor` (and 3 related class filters) | 16/16 Application + 45/45 Integration, green |
| `npx vitest run CuentaCorrienteDeProveedor ResumenSaldoDeProveedor cuentaCorrienteDeProveedor` | 3 files / 37/37 green |
| Migration file read in full (`20260817153958_CuentaCorrienteDeProveedoresEtapa15.cs`) | matches gate sections A-D verbatim |
| `git diff --stat` across all 6 merges (b71a23d^..cbd2788) | `VentasCheckoutTests.cs` and `src/Ways.Application/Ventas/` absent; `ManejadorDeErrores.cs` absent |

## 1. Gate (binding, state.yaml db_gate_approval)

| Criterion | Result |
|---|---|
| Exactly one new migration named `CuentaCorrienteDeProveedoresEtapa15` | PASS - one `.cs` + one `.Designer.cs`, `git log` shows a single migration commit for this stage |
| `dotnet ef migrations has-pending-model-changes` clean | PASS (verified empirically above) |
| Index count = 7 | PASS - 6 named `CreateIndex` on the new table (tenant, proveedor_fecha, comprobante_compra, punto_venta, empleado, gasto) + 1 implicit unique index from `ak_gastos_id_gasto_id_tenant`. No unnamed EF-autogenerated FK-support index found in the migration file |
| Backfill fidelity vs. the retired formula, with soft-deletes and `id_proveedor IS NULL` gasto as discriminants | PASS - `CuentaCorrienteProveedorBackfillTests.cs` seeds a soft-deleted compra, a soft-deleted gasto, a soft-deleted proveedor and an orphan (`id_proveedor IS NULL`) proveedor-categoria gasto, asserts each is excluded, and runs green (part of the 45/45 filtered pass) |
| Zero DDL outside proposal sections A-D | PASS - migration body is exactly: enum annotation, `AddUniqueConstraint` on `gastos`, `CreateTable`, 6 `CreateIndex`, 2 `Sql()` data statements, one `AddColumn` on `proveedores`, `HabilitarRlsDeTenant` last. No other table touched |

Deviation #15 (ALTER-gastos ordering moved before `CreateTable`, not after `ALTER proveedores` as
task 1.10's literal text specified) is true and correctly justified: the shipped migration places
`AddUniqueConstraint` on `gastos` immediately after the enum `AlterDatabase`, before `CreateTable`
- matching what was verified against the file. This is the only DDL-ordering deviation from the
tasks.md literal text and it is registered, evidenced (Postgres `42830` reproduced) and does not
change the model summary the gate binds to.

## 2. Traceability (spec to implementation to test)

Actual counts (measured against the retrieved spec files, not estimated):

| Spec | Requirements | Scenarios |
|---|---|---|
| cuenta-corriente-de-proveedores (NEW) | 11 | 27 |
| saldo-de-proveedor (delta) | 1 MODIFIED + 2 REMOVED | 3 (MODIFIED only; REMOVED carry none) |
| gastos (delta) | 1 ADDED | 3 |
| comprobantes-compra (delta) | 2 ADDED | 2 |
| operacion-de-pos (delta) | 2 ADDED | 3 |
| Total | 17 requirement deltas | 38 scenarios |

All 38 scenarios trace to a passing test, confirmed by direct inspection of the tasks.md test-plan
lines and, for the sampled files, actual test bodies:

- Ledger shape / apertura CHECK -> `CuentaCorrienteProveedorSchemaTests.cs` (23514 raw-insert both
  directions), `CuentaCorrienteProveedorBackfillTests.cs` (apertura row shape).
- Backfill fidelity + idempotency + zero-saldo no-op -> `CuentaCorrienteProveedorBackfillTests.cs`.
- Confirm writes one `compra` movement; anulacion writes the reversing `ajuste` (incl. saldo a
  favor, incl. the pre-cutover fallback OD8) -> `CuentaCorrienteProveedorEscriturasTests.cs`.
- Pago-por-gasto predicate (both directions), arqueo no-regression, `turno_no_abierto` writes
  nothing -> `ServicioDeGastos`-side integration tests (slice 3).
- Manual ajuste (detalle/importe rejections, 403/200 matrix, PV-before-transaction) -> slice-5
  integration tests, `SuperficieDeAutorizacionTests.cs` allowlist growth (+6 lines).
- Per-compra payment status (OD7 formula, both pre-cutover discriminators impaga/parcial) ->
  `SaldoDeProveedorTests.cs` (16/16, byte-identical asserts) + `SaldoDeProveedorReSourceadoTests.cs`
  (345 new lines, tasks 4.11-4.13).
- RLS + 42501 + cross-tenant 404 -> `CuentaCorrienteProveedorSchemaTests.cs` / estado-de-cuenta
  authorization tests.
- Pinned lock order + 3 rendezvous races -> `ServicioDeComprasLockOrderTests.cs` (source-text,
  deviation #22) + `CuentaCorrienteProveedorEscriturasTests.cs`/slice-3 race tests (behavioral).
- Estado de cuenta (paginated per OD9, running balance, tied-fecha tiebreaker, -03:00 offset
  boundary) -> `CuentaCorrienteProveedorEstadoDeCuentaTests.cs` (confirmed the offset test post-fix,
  deviation #32).
- Web: descriptor tests, stale-inside-act, pager edges, saldo a favor, double-click guard ->
  `CuentaCorrienteDeProveedor.test.tsx` / `ResumenSaldoDeProveedor.test.tsx` /
  `cuentaCorrienteDeProveedor.test.ts` (37/37 green, filtered run above).

The 2 REMOVED requirements in saldo-de-proveedor ("Saldo Is A Derived Read, Never Persisted",
"Saldo Is An Approximation, Not An Invariant") are correctly absent from the current spec text and
their replacement invariant (proveedores.saldo as single-write-authority cache) is stated in
cuenta-corriente-de-proveedores and proven by `EscriturasDeCuentaCorrienteProveedorTests.cs`.

## 3. Decisions (proposal's 9 + design's 16 + state.yaml OD7/OD8/OD9)

| Decision | Verified against code |
|---|---|
| Proposal 1 (apertura movement, no synthetic replay, zero-saldo no-op) | PASS - migration `WHERE d.saldo <> 0`, no id_punto_venta/id_empleado on apertura |
| Proposal 2 / OD-verification (payment stays gastos; ak_gastos_id_gasto_id_tenant added) | PASS - migration AddUniqueConstraint present |
| Proposal 3 (reliquidacion out of scope) | PASS - no id_lista_precio reference, no reliquidacion code in the diff |
| Proposal 4 (native 4-value enum, no speculative values) | PASS - tipo_movimiento_cc_proveedor = apertura,compra,pago,ajuste |
| Proposal 5 (contramovimiento on anulacion, gastos not reversed) | PASS - EjecutarAnulacionAsync writes the reversing ajuste; gastosLigados stays informational |
| Proposal 6 (one write authority, EscriturasDeCuentaCorrienteProveedor) | PASS - raw UPDATE...RETURNING confirmed by source-text test (deviation #21), no tracked Saldo += |
| Proposal 7 (imputacion: one column, no FIFO) | PASS - single id_comprobante_compra column, per-tipo meaning |
| Proposal 8 (new policy, not reusing SupervisionDeCuentaCorriente) | PASS - Politicas.SupervisionDeCuentaDeProveedor is a distinct constant/policy registration |
| Proposal 9 (proveedores last row lock) | PASS - verified in ServicioDeCompras.cs inline comments + ServicioDeComprasLockOrderTests.cs |
| OD7 (payment-status formula) | PASS - ServicioDeSaldoDeProveedor.cs implements SUM(gastos.importe) + SUM(-importe) WHERE tipo='ajuste', excluding pago - matches the binding formula verbatim, not the proposal's nor the design's rejected shapes |
| OD8 (anulacion pre-cutover fallback) | PASS - EjecutarAnulacionAsync step 5 falls back to total from the widened RETURNING when no compra movement exists |
| OD9 (paginated estado de cuenta) | PASS - PaginaDeEstadoDeCuentaDeProveedor(Header, Items, Total, Pagina, Tamanio, Historico, Desde, Hasta) exists; OFFSET/Skip/Take present |
| Export route (OD6, T1) OUT of scope | PASS - no /export route in CuentaCorrienteDeProveedorEndpoints.cs |

## 4. Non-regression

| Check | Result |
|---|---|
| VentasCheckoutTests.cs absent from the stage's diff | PASS - git diff --stat across all 6 merges shows zero touches |
| No file under src/Ways.Application/Ventas/ in the diff | PASS - confirmed by the same diff |
| ManejadorDeErrores.cs untouched | PASS - absent from the diff-stat; line 224's generic fk_/23503 mapping still covers this stage |
| SaldoDeProveedorTests.cs (stage-8, 16 tests) intact in its asserts | PASS - the only diff to that file is one MapEnum<TipoMovimientoCcProveedor> line (deviation #28); zero assert lines changed; 16/16 Fact/Theory still present |

## 5. Deviation sample (10 of 44, including the requested set)

All sampled deviations were checked against current code, not taken on faith:

- #15 (ALTER-gastos ordering) - TRUE, verified in the migration file (section 1 above).
- #20 / #28 (missing MapEnum in hand-curated test contexts) - TRUE, SaldoDeProveedorTests.cs's diff
  is exactly the one added MapEnum line.
- #22 (mutation target #19 resolved as source-text, not live rendezvous) - TRUE,
  ServicioDeComprasLockOrderTests.cs exists and reads ServicioDeCompras.cs from disk for a
  text-index assertion; the behavioral race test (2.17) still ships separately.
- #29 (mutation target #25 resolved as source-text) - TRUE,
  ServicioDeCuentaCorrienteDeProveedorLecturaTests.ConstruirQueryDesempataPorIdMovimientoDescendenteTarget25
  reads the source file and asserts ThenByDescending(m => m.Id) chains within 40 chars of
  OrderByDescending(m => m.Fecha).
- #30 (mutation evidence for target #24 tried both rejected formulas) - code-consistent:
  ServicioDeSaldoDeProveedor.cs implements neither rejected shape, only the binding OD7 formula
  (section 3 above); the deviation's narrative cannot be independently replayed by verify (it
  describes a since-reverted mutation cycle) but its claimed end state matches the shipped code
  exactly.
- #31 (3 CRITICAL coverage gaps fixed tests-only in slice 4) - TRUE, all three named tests exist:
  SaldoDeProveedorReSourceadoTests.ElSaldoLeeLaCacheAunDesincronizadaDelAgregadoDelLedger,
  CuentaCorrienteProveedorEstadoDeCuentaTests.LosItemsDevuelvenElSaldoResultanteAcumuladoPorFila,
  CuentaCorrienteProveedorEstadoDeCuentaTests.UnSegundoProveedorDelMismoTenantNoContaminaElEstadoDeCuenta.
- #32 (regla-10 offset test was self-defeating, fixed) - TRUE, the current test
  (UnFiltroHastaConOffsetRealMenosTresIncluyeUnMovimientoQueUnFiltroEnUtcExcluiria) does not send
  historico=true; the boundary dataset (1m59s inside / 5m01s outside) matches the deviation's
  description exactly.
- #37 (corrected) / #44 - TRUE, and the correction is itself verified: ResumenSaldoDeProveedor.tsx's
  Link now passes state={proveedor ? { proveedor } : undefined}, and
  CuentaCorrienteDeProveedor.tsx's puedeAjustar no longer references errorProveedor/proveedorInfo -
  only role + puntos de venta. Both fixes present in code exactly as deviation #44 describes.
- #43 (double-click test was overdetermined, fixed) - TRUE, the current test dispatches both click
  events inside one act() with no await between them, matching the fix's description; the test is
  named "doble click en el mismo tick en Registrar ajuste dispara exactamente un POST".

No falsified deviation found in the sample - every documented reason checks out against the
current codebase (rule 12 satisfied for the sampled set).

## 6. doc-10

docs/10-modelo-de-datos.md section 8b carries the new table with a DDL block that matches the
shipped migration column-for-column; proveedores.saldo in section 2 carries the "cache; el libro
es movimientos_cuenta_corriente_proveedor (section 8, Etapa 15)" annotation; the retired "saldo
derivado, deliberadamente simple" note is replaced with a "Retirado (Etapa 15...)" block. The
"Estado (Etapa 15, ... Slice 1)" annotation names only slice 1's scope ("schema + backfill:
implementada") even though slices 2-6 shipped afterward - the same doc-10-freeze pattern flagged
as WARNING 1 in stage-14's verify report.

## Warnings

WARNING 1 - doc-10's "Estado (Etapa 15)" annotation is frozen at slice 1's scope, the same
recurring class stage-14's verify report flagged (its own WARNING 1). It reads "Slice 1 - schema +
backfill: implementada" and lists the future slices as "llegan en Slice X" in future tense, even
though all 6 slices are merged to main. Remediable pre-archive: the annotation should be updated
to state the whole stage is implemented, mirroring how stage-14's WARNING 1 was closed at archive
time.

WARNING 2 - deviation #30's narrative is not independently replayable by verify. It describes
three mutation-evidence cycles (testing both rejected OD7 formulas against the fixtures, then
reverting) that happened during sdd-apply and are not preserved as a runnable artifact - by
construction, since they were reverted. The end-state code was independently confirmed correct
(sections 3/5 above: ServicioDeSaldoDeProveedor.cs implements exactly the binding OD7 formula), so
this is a process-evidence limitation, not a code defect. Non-blocking; no remediation needed
beyond noting the limitation for future verify passes of stages with heavy mutation-testing
narratives.

## Suggestions

SUGGESTION 1 - tasks.md's "Orchestrator Decisions Recorded This Phase" section overlaps
numerically with the "deviations registered during sdd-apply" list (both share one running 1-44
sequence). This is internally consistent (44 total entries, matches the instruction's count) but
the split between "tasks-phase decisions" (1-14) and "apply-phase deviations" (15-44) is only
discoverable by reading the whole file - a short index at the top of the numbered list would save
a future auditor the same full-file read this verify pass required.

SUGGESTION 2 - Task 4.6 registers, inline, that CuentaCorrienteDeProveedorEndpoints.cs is created
once (slice 4) and only modified in slice 5 rather than being listed as a second Create - this
reconciliation is correct against the actual file history but exists only as prose inside a task
bullet. No action needed, recorded for completeness only.

## Compliance

mutation-proof-tests (28/28 named targets placed and evidenced across the 6 slices, several
correctly escalated to source-text assertions per rule 3 when a live behavioral test was proven
structurally unreachable - targets #4, #11, #17, #19, #25); db-error-backstops (all FK/CHECK
exemptions documented, no new 23505 family, ManejadorDeErrores.cs untouched); dto-contract-honesty
(no accepted-and-dropped field on SolicitudDeAjusteDeProveedor or ContratosDeProveedor.cs; /saldo
response byte-compatible); react-async-state / web-descriptor-tests (slice 6 only, verified green
above); work-unit-commits, judgment-day (clean rounds recorded per slice in state.yaml's apply
notes, with fixes evidenced by mutation in tasks.md).

**Next recommended**: sdd-archive, after remediating WARNING 1 (doc-10 annotation refresh) - the
same non-blocking pattern stage-14 closed at archive time.
**Risks**: none blocking. No CRITICAL found.
