# NOTE (2026-08-02, resolved at archive): the tenant-organization delta spec in this change
# was written against stage 1's change-dir spec BEFORE stage 1 was archived. The delta has
# now been merged into openspec/specs/tenant-organization/spec.md (MODIFIED provisioning
# requirement + ADDED backfill requirement), preserving the stage-1 deviation note. See
# archive-report.md for the full merge record.
change: stage-2-clientes-proveedores
phase: archive
status: archived
artifact_store: openspec
phases:
  explore:
    status: skipped
  propose:
    status: done
    artifact: proposal.md
  spec:
    status: done
    artifact: specs/
  design:
    status: done
    artifact: design.md
  tasks:
    status: done
    artifact: tasks.md
  apply:
    status: done
    artifact: apply-progress.md
    note: >-
      Slice 1 complete and merged to main (PR #10). Slice 2 (clientes) complete
      and merged to main (PR #12), judgment-day APPROVED after 2 rounds. Slice
      3 (apply batch 6, branch feat/stage2-slice3-proveedores off main) done:
      ServicioDeProveedores + ProveedoresEndpoints (list/create/edit/
      soft-delete, GestionDeCatalogo policy), cuit uniqueness proven under
      real concurrency (unconditional INSERT + pre-check, same shape as
      ServicioDeCatalogo — no atomic counter needed, unlike clientes' numero;
      race test needs no forced rendezvous either, same CatalogosTests
      precedent), dedicated Proveedores.tsx web screen wired (route + nav).
      Re-scoped per same precedent as Slice 2: proveedores web ABM pulled
      forward from the original Slice 4 into this Slice 3 batch — Slice 4 as
      a standalone unit is now fully closed (both clientes and proveedores
      screens done). tasks.md has zero unchecked items across Slices 1-4.
      Closed the Slice-2-carried INFO (cross-tenant IdEmpresa pre-check
      symmetry) for BOTH services. One proactive addition beyond the literal
      task list: margen server-side validation (ExigirMargenValido), same
      precedent as clientes' limite_credito judgment-day fix, applied from
      the start here. Full suite runtime-verified twice for stability: Domain
      69/69, Application 126/126, IntegrationTests 126/126 (0 regressions).
      Web: tsc/oxlint/vite build all clean. No DB changes this slice (schema
      from Slice 1 covers everything). Work-unit commits, no push/PR yet —
      awaiting judgment-day (orchestrated separately) before PR.
  verify:
    status: done
    artifact: verify-report.md
    note: >-
      Verdict PASS (2026-08-02, post-fix). 326/326 tests (Domain 69/69, Application 128/128,
      IntegrationTests 129/129), 33/33 spec scenarios compliant. The one CRITICAL finding
      (specs/listas-precio-minimal/spec.md's contradicting "defaults to General list"
      scenario) was fixed in place before archive.
  archive:
    status: done
    artifact: archive-report.md
    note: >-
      Archived to openspec/changes/archive/2026-08-02-stage-2-clientes-proveedores/. Delta
      specs merged into openspec/specs/{clientes,proveedores,listas-precio-minimal}/ (new
      baselines) and openspec/specs/tenant-organization/spec.md (merged delta, stage-1
      deviation note preserved).
dependencies:
  spec: [propose]
  design: [propose]
  tasks: [spec, design]
  apply: [tasks]
  verify: [apply]
  archive: [verify]
notes:
  - "Scope pre-negotiated with the user (2026-08-02) and marked binding by the orchestrator prompt: full clientes schema (doc 10 §2) including complete credit fields at rest (limite_credito/credito_ilimitado/saldo, no CC movement engine — stage 7), atomic per-tenant numero (numeraciones_comprobante pattern family), protected Consumidor Final row seeded for new + existing tenants, full proveedores schema, listas_precio MINIMAL (table + General/es_default list + provisioning template only, no precios/derived lists — stage 3), clientes.id_lista_precio NOT NULL from day one, both tables catalog-scoped (id_tenant + id_empresa NULL), db-error-backstops compliance planned per constraint."
  - "Explicit reuse directive: stage 2 sits on stage-1 machinery (EntidadTenant, generic catalog machine, RLS helpers, keyed platform context, error-code contract) — proposal.md's Approach says reuse, not redesign; design.md must not re-litigate stage-1 ADRs unless a genuine new gap appears."
  - "Out of scope, explicit: precios/ofertas (stage 3), movimientos_cuenta_corriente (stage 7), legacy migration, ADR-10 DeLaEmpresa (stays deferred per stage 1 unless design finds it now necessary for empresa-scoped clientes -- if so, flag as open question, do not silently decide)."
  - "DB CHANGE GATE (CLAUDE.md) applies before any EF Core migration is generated during apply -- same unconditional rule as stage 1."
  - "Delivery: chained PRs, stacked-to-main (session decision, per protocolo-pr-solo-dev). Exact slicing deferred to sdd-tasks Review Workload Forecast."
  - "Proposal question round written into proposal.md ('## Proposal question round') per sdd-propose Step 0 -- not blocking artifact creation since this is a single-shot delegated write, but explicitly flagged for user review before spec/design lock in the resulting assumptions: (1) proveedores.cuit uniqueness undecided by doc 10, (2) clientes.numero_documento uniqueness undecided by doc 10, (3) Consumidor Final backfill-on-existing-tenants timing/approval, (4) ADR-10 DeLaEmpresa re-flagged per scope note. Default assumptions stated in the proposal if no correction arrives."
  - "design.md (2026-08-02): clientes/proveedores do NOT reuse ServicioDeCatalogo<T>/ConfiguracionDeCatalogo<T> (dedicated entity+EF config+service+screen each) -- proveedores' cuit-based tenant-wide uniqueness and clientes' multi-field identity/service-assigned numero break the generic base's hardcoded nombre-pair uniqueness; only listas_precio genuinely fits the generic EF base (no service/API this stage, no ABM in scope). numero assigned via numeraciones_clientes counter table (UPDATE...RETURNING, doc-09 pattern family, keyed by id_tenant) through a shared AsignadorDeNumeroCliente helper reused by CrearAsync, provisioning CF bootstrap, and backfill -- raw ADO.NET, not Database.SqlQuery<T>()/FromSqlRaw<T>() (stage-1 slice-2 confirmed SqlQuery<T>() throws IndexOutOfRangeException against this model). Consumidor Final protected by a domain guard (ServicioDeClientes blocks edit/delete when Numero==1) plus a DB CHECK ck_clientes_cf_protegido (numero<>1 OR deleted_at IS NULL) as the irreversible-delete backstop -- no trigger (no precedent in this codebase). PlantillaDeAprovisionamiento extended in place (fills its own documented ItemsDiferidos gap) rather than bumped to V2 -- ADR-16 versioning is for a different vertical template, not staged completion; flagged as a deviation from the proposal's literal 'versioned bump' wording, not from a resolved binding decision. ADR-10 DeLaEmpresa reconfirmed deferred, no new gap found. One combined migration + one DB CHANGE GATE for all 4 tables (schema + backfill plan together, per resolved decision #3)."
  - "Slice 2 apply batch (2026-08-02): orchestrator prompt re-scoped delivery to a vertical clientes slice (service + API + web ABM together) instead of tasks.md's original layer split (Slice 2 = service/API only, Slice 4 = both screens after Slices 2+3) -- tasks.md updated in place to record this; proveedores stays on its original Slice 3/4 split. Two deviations from tasks.md's literal task wording, both documented with justification in apply-progress.md batch 4: (1) id_lista_precio/id_condicion_fiscal implemented as REQUIRED, not defaulted-when-omitted -- specs/clientes/spec.md's own acceptance scenario contradicted tasks.md 2.1's one-line 'defaults when omitted' phrasing, which design.md never stated either; spec.md treated as the higher-authority contract. (2) task 2.5's race test does not reuse task 1.13's forced rendezvous interceptor -- AsignadorDeNumeroCliente's UPDATE...RETURNING already serializes on Postgres's own row lock (confirmed without forcing anything in Slice 1 batch 3's AsignadorDeNumeroClienteConcurrenciaTests), so two Task.WhenAll-launched POSTs already race for real. Also added a GET /api/listas-precio read-only reference listing (ServicioDeClientes/ClientesEndpoints, not a dedicated listas_precio Servicio/Endpoints) to back the web form's lista-precio selector -- same precedent as /api/roles inside UsuariosEndpoints; listas_precio ABM itself (create/edit/delete) stays out of scope, unchanged from design decision 1."
  - "Judgment-day verdict: Slice 2 (clientes) APPROVED after 2 rounds (2026-08-02). R1: 3 confirmed (untestable-by-design race test replaced with raw-SQL 23505 proof; limite_credito server validation; cross-tenant write 404 parity) + coverage/doc fixes incl. correcting a false deviation rationale. R2: CLEAN x2. Final suite: 69+107+117/117."
  - "Slice 2 INFO carried to slice 3: add the cross-tenant IdEmpresa pre-check test for symmetry (correct by construction, EntidadTenant filter applies; parity with the lista-precio pair). CLOSED in Slice 3 apply batch 6, for both ServicioDeClientesTests and ServicioDeProveedoresTests."
  - "Slice 3 apply batch 6 (proveedores, 2026-08-02): re-scoped delivery to a vertical proveedores slice (service + API + web ABM together), same precedent as Slice 2's clientes re-scoping -- absorbed the proveedores-only portions of the original Slice 4 (tasks 4.1b/4.3b/4.4b). ServicioDeProveedores.CrearAsync needs no atomic counter/transaction (proveedores has no server-assigned sequential field like clientes' numero): unconditional INSERT + pre-check, same shape as ServicioDeCatalogo.CrearAsync -- this also means, unlike ServicioDeClientesTests, ServicioDeProveedoresTests DOES cover CrearAsync fully at the InMemory level (no transaction/raw-ADO blocker). The ux_proveedores_cuit race test (task 3.5) does NOT reuse task 1.13's rendezvous interceptor -- cuit is client-supplied with no atomic counter serializing anything, but the unconditional-INSERT-with-pre-check shape already races for real with a plain Task.WhenAll, confirmed via the exact same precedent CatalogosTests already established in this codebase (run 3x with no flakiness). tasks.md's Slices 1-4 have zero unchecked items after this batch. Full suite runtime-verified twice for stability: 69/126/126, 0 regressions."
  - "Judgment-day ronda 1 de Slice 3 (2026-08-02), INFO registrado: la deduplicación de cuit es sensible al formato -- \"20-12345678-9\" y \"20123456789\" se tratan como strings distintos, así que no colisionan aunque representen el mismo CUIT. Esto es consistente con el criterio ya aceptado para numero_documento (mismo comportamiento, sin canonicalizar). Se documenta como limitación aceptada, no como bug: una normalización solo-dígitos es una decisión de producto futura, fuera del alcance de esta slice (NO schema changes, sin nueva validación de negocio pedida)."
  - "Judgment-day verdict: Slice 3 (proveedores) APPROVED after 2 rounds (2026-08-02). R1: 1 confirmed real (numeric precision bounds -> added margen/limite_credito caps + generic 22003->400 backstop proven end-to-end via grupos) + bookkeeping fix. R2: CLEAN x2. Final suite: 69+128+129/129. All tasks of slices 1-4 complete."
  - "Verify verdict (2026-08-02): PASS after fixing 1 CRITICAL spec-coherence issue (listas-precio-minimal default-on-omit scenario superseded in place to match the required-fields contract). Suite 326/326 (69+128+129), tsc clean. INFO for stage 3: explicit 404 regression test for the nonexistent listas_precio write endpoints lands with the listas ABM."
