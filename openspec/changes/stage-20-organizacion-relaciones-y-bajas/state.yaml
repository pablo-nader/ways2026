change: stage-20-organizacion-relaciones-y-bajas
phase: propose
status: done
artifact_store: hybrid
execution_mode: auto
delivery_strategy: auto-chain
chain_strategy: stacked-to-main
review_budget_lines: 800
db_gate: ZERO-SCHEMA-RATIFIED
db_gate_approval: >-
  RATIFIED 2026-09-04 by the orchestrator under the owner's explicit autonomous
  mandate ("hace todo en modo autonomo"). RATIFICATION BASIS: CLAUDE.md's
  mandatory DB gate governs "creating or modifying ANY database schema". This
  stage creates and modifies NONE, so the gate is NOT TRIGGERED — there is no
  model summary to present and nothing for the owner to approve. The four
  binding verify criteria below are what keep that true; if ANY later phase
  proposes DDL, a migration, a data statement or a seed change, the gate
  REOPENS immediately and work STOPS for owner approval before that change is
  written. The orchestrator independently verified the three load-bearing
  claims in the tree before ratifying: EstadoTenant.Baja exists in
  src/Ways.Domain/Organizacion/EstadoTenant.cs with the doc comment "Dado de
  baja. No se elimina fisicamente: los datos quedan para exportacion";
  20260801011312_Organizacion.cs:18 declares the enum as
  'activo,baja,suspendido'; and IWaysDbContext.cs:153 exposes DatabaseFacade
  but not IModel.
  ---
  PROPOSED 2026-09-04 by sdd-propose under the delegated autonomous mandate:
  ZERO SCHEMA CHANGES for the whole stage — zero migrations, zero tables,
  zero columns, zero enum values, zero ALTER, zero indexes, zero data
  statements, zero seed changes. AWAITING OWNER RATIFICATION before slice 1.
  BASIS (each item verified in the tree, not assumed): (1) deleted_at already
  exists on tenants, empresas, puntos_venta and usuarios via EntidadBase, and
  is configured per entity (TenantConfiguration.cs:33); (2) the "BajaLogica"
  query filter is already installed on every EntidadBase descendant
  (WaysDbContext.AplicarFiltroDeBajaLogica, :426-447); (3) the native
  PostgreSQL enum estado_tenant was created with ALL THREE values
  'activo,baja,suspendido' in the FIRST migration
  (20260801011312_Organizacion.cs:18) — 'baja' is already in the database and
  only its WRITER was missing, so decision 1 adds a writer, not a value;
  (4) owner names and child counts are read-only projections over existing
  FKs, and a projection is not a schema change. The section
  'Modelo de datos propuesto — CERO CAMBIOS DE SCHEMA' of proposal.md IS THE
  CONTRACT: any DDL or data statement proposed by a later phase is a scope
  violation that REOPENS the gate and stops work for owner approval.
  BINDING VERIFY CRITERIA: (a) no new file under
  src/Ways.Infrastructure/Persistencia/Migraciones/ — the last migration of
  the tree at propose time must STILL be the last one at verify;
  (b) 'dotnet ef migrations has-pending-model-changes' clean;
  (c) InicializadorDeBaseDeDatos.cs untouched; (d) zero physical deletes in
  the diff (repository scan for ExecuteDelete, Remove(, RemoveRange( and
  'DELETE FROM' over the four tables) — binding owner decision B1.
  AUTHORIZATION SURFACE: Politicas.cs UNTOUCHED — all four DELETE routes
  reuse the policy of the group they already belong to (SoloPlataforma,
  GestionDeOrganizacion, and the existing usuarios policy). Zero new
  policies. Precedent for a zero-DDL stage: stages 10, 11, 13 and 18.
orchestrator_decisions:
  OD1: >-
    SESSION PREFLIGHT (decided before propose, not reopened by any phase):
    execution_mode=auto, artifact_store=hybrid (openspec files AND Engram via
    mem_save with capture_prompt:false), delivery_strategy=auto-chain,
    chain_strategy=stacked-to-main, review_budget_lines=800. The 800-line
    budget replaces the 400-line default of sdd-phase-common section E for
    this change only; the five forecast slices all sit at roughly half of it,
    so the budget risk is LOW and three split points are pre-authorized.
  OD2: >-
    LANGUAGE CONTRACT: every SDD artifact of this change is written in
    ENGLISH (proposal.md, state.yaml, specs, design, tasks, reports).
    C#/TS identifiers are English; database objects follow the Spanish
    snake_case convention of docs/10-modelo-de-datos.md; user-facing API
    messages and web copy stay in Spanish, following the target context
    (the six new 409 messages are Spanish strings, their CODES are Spanish
    snake_case per the existing ErrorDominio idiom).
  OD3: >-
    SCOPE SHAPE: the stage is TWO coherent parts under one change — Part A
    (project the organization hierarchy in the root UI: owner names instead
    of raw ids, a tenant column on usuarios, tenant/empresa filters, child
    counts on tenants) and Part B (usage-guarded logical deletion for tenant,
    empresa, punto de venta, plus alignment of the existing but unguarded
    Usuario deletion). Slices 1-2 deliver Part A and 3-5 deliver Part B, and
    they are INDEPENDENT ON PURPOSE: Part A ships standalone value if Part B
    stalls, and slice 3 (the guard) is deliberately INERT — it has no caller
    until slice 4 — so the guard can be reviewed on its own merits before
    anything can invoke it.
  OD4: >-
    T3 ARBITRATED — A SOFT-DELETED DEPENDENT STILL BLOCKS. The sdd-design
    launch prompt written by the orchestrator contained the line "a logically
    deleted child must NOT block deletion", which CONTRADICTS what proposal.md
    and the spec had already settled. The contradiction was the orchestrator's
    own drafting error, not an open product question, and it is resolved in
    favour of the proposal: a dependent the customer created and later deleted
    STILL blocks. RATIONALE: the owner's binding words were "lo que sea que el
    usuario ya haya operado" — the customer operated; deleting the row
    afterwards does not rewind that history. It is also the fail-safe
    direction (it over-blocks), and the permissive reading would let anyone
    mass-delete their data and then delete the entity that held it. This is a
    ONE-LINE REVERSIBLE KNOB, recorded as such: reversing it means adding
    "AND d.deleted_at IS NULL" per branch, flipping one test, and regenerating
    the N3 inventory golden. Note this rule is NOT in tension with S2 — the
    usage guard counts history (deleted dependents count) while the structural
    minimum counts live siblings (deleted siblings do not). Two different
    questions, both failing safe.
  OD5: >-
    T1 ACCEPTED AS A KNOWN LATENCY, SCOPE NOT EXPANDED. Design established
    that Ways has NO endpoint creating a second empresa or punto de venta —
    OrganizacionEndpoints.cs exposes only GET and PUT for both, and
    ServicioDeAprovisionamiento creates exactly one of each. Consequence: the
    structural minimum fires on EVERY empresa/PV delete attempt through the
    API, so empresa_en_uso and punto_venta_en_uso are reachable only below the
    API layer, and empresa/PV deletion ships LATENT — correct, tested, and
    unreachable until creation endpoints exist. This is the same shape as
    EstadoTenant.Baja, which shipped in stage 1 and waited until now for its
    writer. The orchestrator did NOT add creation endpoints: the owner asked
    to be able to DELETE things created by mistake, not to create more, and
    silently widening scope is the owner's call to make, not the
    orchestrator's. sdd-tasks MUST NOT write API-level tests for those two
    codes, and the latency MUST be reported to the owner at delivery.
  OD6: >-
    T2 ACCEPTED — design's correction stands over the proposal. A
    cascade-deleted admin receives 401 credenciales_invalidas, NOT 403
    tenant_suspendido: the login lookup runs under the "BajaLogica" query
    filter with no IgnoreQueryFilters, so the user is simply not found.
    Orchestrator verified ServicioDeAutenticacion.cs:74-75 declares
    credenciales_invalidas with status 401. The proposal's Success Criterion
    is wrong and design's version is the one tasks must implement.
phases:
  explore:
    status: done
    date: 2026-09-04
    artifact: explore.md
    depends_on: []
    notes: >-
      Triggered by the owner testing the running application: the root admin
      surface shows tenants, empresas, puntos de venta and usuarios as four
      unrelated flat lists, and nothing can be deleted except suspending a
      tenant. VERIFIED, not assumed: the relationships are in the DTOs but
      rendered as raw integers (Empresas.tsx:156, PuntosVenta.tsx:216-217);
      UsuarioListado carries NO tenant field at all (Contratos.cs:19-27);
      there is NO DELETE for tenant/empresa/punto de venta anywhere in the
      API surface — OrganizacionEndpoints.cs:12-13 defers alta/baja to
      ServicioDeAprovisionamiento (ADR-16) but AprovisionamientoEndpoints.cs
      exposes ONLY POST /api/plataforma/tenants, so the delete path that
      comment defers to was never implemented. CORRECTED THE OWNER'S REPORT
      on one point: Usuario DOES already delete logically
      (UsuariosEndpoints.cs:57, wired at Usuarios.tsx:120-122) — its gap is
      the absence of any dependent guard, not the absence of the endpoint.
      CENTRAL FINDING: every delete in this codebase is logical, so
      DeleteBehavior.Restrict contributes ZERO protection and any
      "blocked when in use" rule must be an explicit application check.
      Three guard options compared; EF metadata via
      IEntityType.GetReferencingForeignKeys() selected because a hand list
      FAILS UNSAFE while metadata FAILS SAFE. Left three open questions
      (cascade, structural minimums, Usuario guard), all recommended yes.
  propose:
    status: done
    date: 2026-09-04
    artifact: proposal.md
    depends_on: [explore]
    notes: >-
      Nine numbered decisions: the explore's three open questions resolved
      (cascade YES with a boundary, structural minimums YES with an ordering
      rule, Usuario guard YES positioned after PoliticaDeRoles) plus six the
      proposal had to take. NEW MATERIAL THE EXPLORE DID NOT FIND, in order
      of impact: (a) EstadoTenant.Baja is a LATENT VALUE WITH ZERO WRITERS —
      the enum declares it (EstadoTenant.cs:16), Tenant.PuedeOperar reads it,
      login rejects it (ServicioDeAutenticacion.cs:141),
      CambiarEstadoTenantAsync refuses to touch it
      (ServicioDeOrganizacion.cs:67) and Tenants.tsx:12 documents its
      absence, but 'Estado = EstadoTenant.' resolves ONLY to Activo, twice —
      this is stage 17's "PRE latente" pattern already in the tree, and
      tenant deletion is its natural writer (decision 1); (b) the B3
      discriminator IS NOT UNIFORMLY APPLICABLE — 15 domain types do not
      inherit EntidadBase and have NO created_at column at all (Stock,
      StockLote, MovimientoStock, MovimientoCaja, MovimientoTesoreria,
      ArqueoTurno, MovimientoCuentaCorriente,
      MovimientoCuentaCorrienteProveedor, Auditoria, ArticuloEmpresa,
      OfertaLista and the four Numeracion*), verified by reading Stock.cs and
      ArticuloEmpresa.cs which carry no timestamp property whatsoever — hence
      decision 4's THREE BUCKETS (timestamped / untimestamped / carve-out)
      and the COMPLETENESS TEST that converts the "fails safe" argument into
      a guarantee by failing the build on any unclassified dependent type;
      (c) soft-deleting a tenant already degrades login correctly to 403
      through the EXISTING 'tenant is null' branch, so decision 1 needs no
      auth change; (d) raw parameterised SQL gives the DESIRED filter-bypass
      semantics for free (a soft-deleted dependent still blocks, which is the
      strictest reading of B2) while RLS still applies at the connection
      level, so the guard cannot under-count for a tenant admin; (e)
      IWaysDbContext exposes DatabaseFacade but NOT IModel
      (IWaysDbContext.cs:153) — the interface's own doc-comment (:150-152)
      supplies the exact argument for adding IModel, flagged for sdd-design
      to confirm or replace with a dedicated port; (f) nullable IdEmpresa
      shared-catalogue rows need no special case because fk = @id does not
      match NULL; (g) NO Vitest file exists for ANY of the four screens, so
      Part A CREATES coverage rather than extending it; (h) db-error-backstops
      is STRUCTURALLY INAPPLICABLE (no physical DELETE means no SQLSTATE
      23503 can ever fire), which promotes the guard's tests from good
      practice to SOLE DEFENCE and is recorded as the stage's top risk.
      Six new 409 codes proposed: tenant_en_uso, empresa_en_uso,
      punto_venta_en_uso, usuario_en_uso, ultima_empresa_del_tenant,
      ultimo_punto_venta_de_la_empresa. Plan: 5 slices stacked-to-main
      (~2080 lines), budget risk LOW against 800, three split points
      pre-authorized, four items never degradable.
  spec:
    status: done
    date: 2026-09-04
    artifact: specs/
    depends_on: [propose]
    notes: >-
      Three capability specs written under specs/: bajas-de-organizacion (NEW,
      full spec — 12 requirements, 41 scenarios), tenant-organization (DELTA,
      5 ADDED requirements, 21 scenarios) and usuarios-tenant-scoping (DELTA,
      2 ADDED requirements, 11 scenarios). Total 19 requirements / 73
      scenarios. NOTHING WAS REMOVED, MODIFIED OR RENAMED in either delta —
      every existing requirement of both capabilities stays byte-identical, so
      archive replaces nothing. THE COMPLETENESS RULE IS SPECIFIED AS A
      REQUIREMENT, NOT A CLAIM ("Every Referencing Type Is Classified Into
      Exactly One Bucket Or The Build Fails"): a type in NO bucket fails the
      test naming it, a type in MORE THAN ONE bucket also fails, and skipping
      or defaulting an unclassified type is a spec violation — that is what
      converts B4's "fails safe" into a guarantee given that
      db-error-backstops is structurally N/A. SPEC-LEVEL DECISIONS taken where
      the proposal was silent, each cheap to reverse: (S1) NombreTenant on
      UsuarioListado is NULL exactly when IdTenant is NULL and the literal
      "Plataforma" is rendered by the WEB, not fabricated by the API
      (dto-contract-honesty: the DTO carries the truth, the screen carries the
      copy); (S2) the structural-minimum COUNT excludes logically deleted
      siblings, so a tenant whose second empresa was already deleted still
      gets ultima_empresa_del_tenant on the survivor; (S3) the cascade MUST
      NOT re-stamp a child that was already logically deleted — it keeps its
      ORIGINAL deleted_at, otherwise the shared-instant restore rule of
      decision 2 would be destroyed for the earlier deletion; (S4) reactivate
      on a deleted tenant is a 404 (BajaLogica hides the row from
      BuscarTenantAsync) and the pre-existing 409 tenant_dado_de_baja stays as
      an unreachable backstop — verified in ServicioDeOrganizacion.cs:62-72;
      (S5) filter option sets are derived from the rows the actor can already
      see, so a filter can never disclose an out-of-scope tenant name;
      (S6) when a structural minimum AND usage both apply, the structural code
      wins (decision 3's ordering, made observable). ANTI-ORACLE encoded twice
      (bajas-de-organizacion and usuarios-tenant-scoping): an out-of-scope
      DELETE is 404, never 403 and never a 409 that discloses usage. ZERO
      SCHEMA is preserved — no requirement asks for a column, a table, an
      index or a migration.
  design:
    status: done
    date: 2026-09-04
    artifact: design.md
    depends_on: [propose]
    notes: >-
      Fifteen decisions (D1-D15) plus the seven arbitrations the prompt named
      (A untimestamped types, B the completeness test, C IModel access, D query
      cost, E query filters, F cascade atomicity, G concurrency). NINE
      STRUCTURAL FACTS VERIFIED IN THE TREE, three of which CORRECT the
      proposal: (1) Empresa/PuntoVenta carry NO navigation properties
      (Empresa.cs:9-26), so owner names can only be correlated scalar
      subqueries — which also removes the INNER-JOIN-drops-the-row trap;
      (2) NO ENDPOINT CREATES A SECOND EMPRESA OR PUNTO DE VENTA
      (AprovisionamientoEndpoints exposes only POST /api/plataforma/tenants and
      OrganizacionEndpoints has no POST), so decision 3's structural minimums
      fire on EVERY empresa/PV delete attempt and empresa_en_uso /
      punto_venta_en_uso are UNREACHABLE THROUGH THE API today — their tests
      must be below-the-confound service-level tests (T1, mutation-proof rule
      3); (3) a cascade-deleted admin gets 401 credenciales_invalidas, NOT the
      403 tenant_suspendido the proposal's Success Criteria claims — the login
      lookup runs under "BajaLogica" (ServicioDeAutenticacion.cs:85-87, no
      IgnoreQueryFilters) so the user is not found and dies at :104; the 403
      branch stays reachable only for a SUSPENDED tenant (T2). C RESOLVED:
      expose IModel on IWaysDbContext — blast radius VERIFIED as ONE LINE
      (rg ": IWaysDbContext" over src/ and tests/ returns ZERO matches; no
      hand-written implementation, no fake, no mock exists), and DbContext.Model
      already satisfies it implicitly; a dedicated port was rejected because its
      only implementation would be "=> contexto.Model". B RESOLVED HONESTLY:
      the classifier is TOTAL by construction (carve-out -> timestamped ->
      untimestamped), so the proposal's literal "fails the build on an
      unclassified type" would be a TAUTOLOGY forbidden by mutation-proof rule
      1 — replaced by FOUR NAMED NETS: N1 totality (Construir throws NAMING the
      type on the three mechanical impossibilities), N2 the bucket is read off
      the TABLE (created_at column) not restated from the code, N3 a CHECKED-IN
      SORTED GOLDEN INVENTORY of every (anchor, table, columns, bucket) that
      goes red with a diff naming any FK a future stage adds/removes/reclassifies
      — this is the executable form of the proposal's intent — and N4 the
      pristine regression. N1-N3 need NO container: they build the real Npgsql
      model over an unopened connection, the existing
      tests/Ways.Application.Tests/Persistencia/Modelo*Tests.cs pattern.
      D RESOLVED: one UNION ALL statement with an outer LIMIT 1 (the Append node
      stops at the first blocking branch); ~40 sequential AnyAsync rejected on
      round trips; accepted honestly for an admin-only action, with a read-only
      pg_indexes coverage check that REPORTS an uncovered branch but cannot fix
      it (ZERO-SCHEMA). F RESOLVED: one ExecutionStrategy transaction, one
      advisory lock, ONE evaluation of the guard (no pre-check — mutation-proof
      rule 3 names the pre-check/guard duality as this repo's most common
      confound), one reloj.Ahora shared by parent and every child on DeletedAt
      AND UpdatedAt, Estado = Baja in the same SaveChangesAsync; write ORDER is
      deliberately NOT claimed (EF owns it inside one SaveChanges) — atomicity
      is the property. G RESOLVED: pg_advisory_xact_lock(idTenant, -20) keyed on
      the TENANT, not the entity, with a NEGATIVE constant because the two-int
      advisory keyspace is already shared with ServicioDeOfertas (idTenant,
      idOferta) and ServicioDePrecios (idTenant, par), both positive; the POS
      takes no lock of this family, so the lock sets are DISJOINT from the total
      order (numeraciones_fiscales -> turnos_caja -> ... -> ledger) and no
      deadlock is expressible. RESIDUAL RACE R1 STATED AND ACCEPTED: a sale
      committed between the guard's read and the deletion's commit yields a
      soft-deleted tenant owning a post-anchor comprobante — closing it would
      put a platform-admin lock into the POS hot path (the stage-19a D1 lesson),
      and B1 makes recovery a one-line UPDATE. Usuario deletion takes NO
      transaction and NO lock (D12): no cascade, and the lock closes no race it
      faces. BUDGETED COST NAMED, NOT DISCOVERED: the guard's raw SQL cannot run
      on the InMemory provider, so the EliminarAsync cases of
      ServicioDeOrganizacionTests and ServicioDeUsuariosTests RELOCATE to the
      integration suite — the exact ServicioDeOfertas.ActualizarAsync precedent
      (ServicioDeOfertas.cs:43-46). TENSION T3 REQUIRING AN OWNER RULING BEFORE
      SLICE 3: the launch prompt says "a logically deleted child must NOT block
      deletion" while the proposal settled the OPPOSITE (decision 6 side effect
      A, question round item 2) and requires a test proving it (proposal.md:506).
      The design IMPLEMENTS THE PROPOSAL per the binding-contract rule and
      records the exact reversal (add AND d.deleted_at IS NULL to every branch,
      flip one test). db-error-backstops declared STRUCTURALLY N/A with
      ManejadorDeErrores.cs untouched as verify criterion V6. Threat matrix N/A;
      the one new boundary (generated SQL) is closed by metadata-only
      pattern-validated identifiers, bound parameters and a read-only statement.
      Twelve binding verify criteria; U1-U8 guarded-write conjuncts enumerated
      up front; five slices unchanged from the proposal with every design
      element assigned a Slice column, budget risk LOW against 800.
  tasks:
    status: done
    date: 2026-09-04
    artifact: tasks.md
    depends_on: [spec, design]
    notes: >-
      Binding breakdown: 5 slices, 103 tasks (16/16/20/39/12), stacked-to-main, merge order
      1 -> 2 -> 3 -> 4 -> 5. Per-slice estimates: 1 ~390, 2 ~430, 3 ~470,
      4 ~530, 5 ~340 (total ~2160). Budget risk LOW against 800; every slice
      sits at roughly half the cap and the three design split points are
      carried as pre-approved degradation. Decision needed before apply: NO
      (the db_gate is ZERO-SCHEMA-RATIFIED). THE THREE ORCHESTRATOR
      ARBITRATIONS ARE TRANSCRIBED AS BINDING NOTES AT THE TOP AND IN THE
      SLICE THAT OWNS THEM: OD4 (a soft-deleted dependent STILL blocks — no
      'AND d.deleted_at IS NULL' branch anywhere; asserted at rendering level
      in task 3.14 and behaviourally in task 4.15; recorded as a one-line
      reversible knob in InspectorDeUso's doc-comment), OD5 (NO API-level test
      for empresa_en_uso / punto_venta_en_uso — they are proven below the API
      with a hand-seeded second empresa/PV in tasks 4.12/4.21/4.26; no creation
      endpoint is added; the latency is reported to the owner at delivery in
      task 5.12) and OD6 (a cascade-deleted admin gets 401
      credenciales_invalidas, task 4.31, with the 403 tenant_suspendido branch
      re-asserted as a regression for a SUSPENDED tenant). SIX RECONCILIACIONES
      REGISTERED, the load-bearing one being that the tenant-organization
      scenario 'a deleted tenant's user ... gets a clean 403' is SUPERSEDED by
      OD6 — the spec text is left byte-identical and verify must read it through
      Reconciliación 1 rather than as a failed criterion. Design's T3 is closed
      by OD4, T1 by OD5, T4 adopted as the four nets N1-N4 (N3's checked-in
      sorted golden inventory is the actual trip-wire, and the proposal's
      literal 'fails the build on an unclassified type' is delivered by it
      rather than by a tautological bucket-membership assertion). N1-N4, the two
      carve-out tests, the 'one article blocks' test and the zero-physical-delete
      scan are carried as NEVER-DEGRADABLE tasks. U1-U8 transcribed verbatim
      into slice 4's header before any test is written (mutation-proof rule 3),
      each paired with its own task-level kill. The EliminarAsync test
      relocation to the integration suite is BUDGETED in slice 4's ~530 (design
      said ~490) and owned by task 4.10, which also requires the PR body to
      state that every moved case is behaviour-identical. 13 binding verify
      criteria carried to every slice, including the ZERO-SCHEMA set (last
      migration stays 20260822002214_FiscalArcaEtapa19a.cs,
      has-pending-model-changes clean, InicializadorDeBaseDeDatos.cs /
      Politicas.cs / ManejadorDeErrores.cs untouched, zero physical deletes).
      db-error-backstops declared STRUCTURALLY N/A in the header and per slice.
      The per-slice delivery ritual is stated explicitly so apply follows it:
      implement -> all relevant test commands green -> judgment-day dual blind
      review to a clean round -> PR (branch-pr, conventional commits, no AI
      attribution) -> merge -> next slice. Integration-suite discipline
      restated as binding: Docker required, NEVER two integration suites
      concurrently against the same daemon.
  apply:
    status: in_progress
    date: 2026-09-04
    artifact: tasks.md + PRs
    depends_on: [tasks]
    notes: >-
      SLICE 1 IMPLEMENTED (tasks 1.1-1.14 checked; 1.15 judgment-day and 1.16 PR
      belong to the orchestrator). Branch feat/stage20-slice1-proyeccion-api off
      main, measured with 'git diff main --stat -- src tests' and NOT estimated.
      After judgment-day round 1, 1285 changed lines (1252 insertions plus 33
      deletions) against the 800-line budget, with
      tests/Ways.IntegrationTests/ProyeccionDeOrganizacionTests.cs at 1076 lines
      and 11 [Fact] methods. After judgment-day round 2 (final), 1420 changed
      lines (1386 insertions plus 34 deletions), of which production is 217 (185
      plus 32) across four files and tests are 1203, with that same test file at
      1194 lines and 12 [Fact] methods. ORCHESTRATOR DECISION, recorded verbatim
      at the round-1 figures it was taken on - slice 1 ships as ONE PR at 1285
      changed lines against the 800-line budget (OD1), because production is only
      ~176 lines and the ~1085 test lines are the evidence round 1's review
      demanded for exactly those 176; splitting would separate the proof from the
      claim and buy two more review cycles for no gain in review quality. The
      owner was told the corrected number and offered the split. Six files:
      Organizacion/Contratos.cs,
      Organizacion/ServicioDeOrganizacion.cs, Usuarios/Contratos.cs,
      Usuarios/ServicioDeUsuarios.cs, WaysApiFixture.cs (one optional params
      IInterceptor[] parameter, zero call sites changed) and the new
      tests/Ways.IntegrationTests/ProyeccionDeOrganizacionTests.cs.
      ZERO-SCHEMA HELD AND VERIFIED, not assumed: zero files under Migraciones/
      in the diff, the last migration is still 20260822002214_FiscalArcaEtapa19a,
      'dotnet ef migrations has-pending-model-changes' reports "No changes have
      been made to the model since the last migration",
      InicializadorDeBaseDeDatos.cs / Politicas.cs / ManejadorDeErrores.cs
      untouched (git diff main --name-only returns them nowhere), zero
      ExecuteDelete/Remove(/RemoveRange(/DELETE FROM added, zero DDL. SUITES:
      dotnet build Ways.slnx clean (2 pre-existing NU1903 warnings on SSH.NET),
      Domain 545/545, Application 373/373, Integration 1732/1732 in 18m08s, run
      once and alone against the Docker daemon per the binding discipline (trx at
      tests/Ways.IntegrationTests/TestResults/slice1.trx). SIX MUTATIONS RUN FOR
      REAL (M1-M6, table in tasks.md): each applied, the named test observed RED
      with its exact failure message, reverted, observed GREEN. Task 1.13 ships
      WITHOUT mutation evidence ON PURPOSE and says so: it is a regression over
      the pre-existing "Tenant" query filter plus RLS, not over a clause this
      slice introduces. TWO DEFECTS IN THE TESTS THEMSELVES were found by running
      the mutations rather than reasoning about them, and both were fixed: (a)
      task 1.11's pairwise-distinctness rested on an order-dependent id > 4
      assertion — now guaranteed by construction with three deliberately
      unbalanced filler tenants that desynchronise the four identity sequences;
      (b) task 1.12's first draft asserted S1's iff over every row and went red
      against the orphan seeded by task 1.10 — the iff holds for the
      platform/tenant distinction but NOT for the D13 orphan, so the assertion
      now states the direction the API actually owes. JUDGMENT CALL RECORDED, NOT
      SILENT: the four listing records are also returned by the detail/edit
      endpoints, and neither the spec nor the design says what those must carry.
      Filling them with 0/null would be exactly the accepted-and-dropped shape
      dto-contract-honesty forbids, so Obtener*/Actualizar*/Suspender/Reactivar
      re-project. The three Obtener* still cost ONE round trip (they project
      instead of loading the entity and validate scope on the projected IdTenant,
      preserving BuscarX's 404-then-ADR-8 order); the four write paths cost one
      extra read. The single-round-trip budget of TO-R1/UT-R1 is about the
      LISTINGS, which are the ones that scale with row count. JUDGMENT-DAY ROUND
      1 (task 1.15) CORRECTED SIX CONFIRMED FINDINGS, three of them with new
      mutation evidence (M7-M9 in tasks.md): (1) IgnoreQueryFilters is applied at
      QUERY level, so incluirEliminados=true also stripped the soft-delete filter
      from the correlated tenant-name subquery and the same account showed a
      soft-deleted tenant's name there while returning null on the other two
      paths — the subquery now carries an explicit t.DeletedAt == null and the
      ADR-6 IgnoreQueryFilters call is untouched; (2) every non-listing response
      path shipped untested (return null; in NombreDeTenantAsync left the whole
      suite green), closed with three readback tests over
      Obtener/Actualizar/Suspender/Reactivar of tenant, Obtener/Actualizar of
      empresa and punto de venta, and the POST 201 / GET {id} / PUT bodies of
      usuario, with pairwise-distinct values per rule 12b; (3) the three
      re-projections after SaveChangesAsync used FirstAsync (500) where the
      sibling read paths return a domain 404 — all three now use
      FirstOrDefaultAsync + ErrorDominio.NoEncontrado, which is what slice 4's
      concurrent deleters need; (4) V9 IS NOT LITERALLY SATISFIED FOR USUARIOS
      and is recorded as Reconciliación 8 in tasks.md instead of being patched
      away: the criterion means "the projection adds no round trip", the usuarios
      listing costs 2 because it is the only PAGINATED listing, and that second
      CountAsync predates this change; the pagination was NOT touched and
      specs/usuarios-tenant-scoping/spec.md:14-15 and :29-32 are left
      byte-identical and marked superseded, exactly as Reconciliación 1 handles
      OD6; (5) RazonSocialEmpresa's e.Id == p.IdEmpresa correlation had no
      deterministic kill because every fixture tenant owned exactly one empresa —
      task 1.8 now seeds a second empresa OF THE SAME TENANT with its own punto
      de venta (rule 12c); (6) the NombreTenant doc-comments published a false
      contract (null iff IdTenant is null) that the D13 orphan violates — both
      Contratos.cs and NombreDeTenantAsync now state the two real cases, and the
      never-fabricate-"Plataforma" rule is unchanged. ZERO SCHEMA HELD ACROSS THE
      CORRECTION: no migration, no DDL, InicializadorDeBaseDeDatos.cs /
      Politicas.cs / ManejadorDeErrores.cs still untouched. PR #165 merged to
      main as commit 5f0018f, so tasks 1.15 and 1.16 are now checked. SLICE 2
      IMPLEMENTED (tasks 2.1-2.14 checked; 2.15 judgment-day and 2.16 PR belong
      to the orchestrator). Branch feat/stage20-slice2-web-relaciones off main -
      the launch prompt's branch name, not the planning name
      feat/stage20-slice2-proyeccion-web that tasks.md still records; same slice,
      different label. Six work-unit commits, web-only: tipos.ts mirror plus 23
      pre-existing fixture files, the five pure helpers plus organizacion.test.ts,
      then one commit per screen (Tenants, Empresas, PuntosVenta, Usuarios).
      BUDGET OVERFLOW MEASURED AND REPORTED, NOT ABSORBED and NOT split
      unilaterally - 'git diff main --stat -- src' gives 34 files, 1909
      insertions plus 289 deletions = 2198 changed lines against an estimate of
      ~430 and the 800-line budget (OD1); ignoring pure re-indentation
      ('git diff -w') it is 1741 plus 121 = 1862; production is ~1054 and tests
      are ~1144, and the four screens alone are 443 insertions plus 120 deletions
      with -w. Same shape the orchestrator already ruled on for slice 1: a small
      production surface carrying the evidence web-descriptor-tests and
      mutation-proof-tests demand (the five new test files are 1078 lines). THE
      PRE-APPROVED 2a/2b DEGRADATION IS NO LONGER A CLEAN CUT and this is stated
      rather than forced: names, filters and the react-async-state retrofit live
      in the same four screen bodies, so a names-vs-filters cut would split single
      functions across two PRs; the clean cut is per screen and the six commits
      are already shaped for it. THIRTEEN MUTATIONS RUN FOR REAL (M1-M13, table
      in tasks.md): twelve killed with their exact failure messages recorded, and
      M10 SURVIVED and is recorded as a survivor rather than dressed up as a kill
      - deleting every generation guard from Empresas.tsx left 6/6 green, because
      react-async-state rule 9 (block every action that could supersede an
      in-flight operation) already closes that window on the three organization
      screens; the guards stay as defence-in-depth for slice 5's delete buttons.
      Usuarios.tsx is the exception and M11 kills there: its search box is
      reachable while a load is in flight, a genuine two-reads-in-flight window,
      and it has its own stale-response test written per mutation-proof rule 7
      (the stale promise is resolved inside act and the assertion is synchronous
      after the flush, so a retrying matcher cannot pass on its first tick). ONE
      TEST DEFECT was found by running M4 and fixed rather than rationalised: the
      empresa-clearing test asserted only toHaveValue('') after switching tenants,
      which the derived empresaVigente fallback satisfies on its own - it now
      returns the tenant filter to "Todos" and requires the full list back, which
      only a genuine setFiltroEmpresa(SIN_FILTRO) produces. RECONCILIACION 9
      HONOURED IN THE WEB: etiquetaDeTenant discriminates on idTenant and never on
      the name, so a null name with a present idTenant renders as the D13 orphan
      marker and never as "Plataforma"; M1 kills that on all four surfaces. The
      "Plataforma" literal is supplied by the web (D14) and a tenant literally
      named "Plataforma" stays distinguishable in the filter by key (the
      sin-tenant token, which no String(idTenant) can produce) and by label (the
      "(sin tenant)" suffix); M2 kills the key collapse. TASK 2.3 HAD NO FILE TO
      MODIFY - src/Ways.Web/src/api/usuarios.ts does not exist, UsuarioListado
      lives in tipos.ts and Usuarios.tsx calls api.get directly; its substance is
      delivered by task 2.1 and no empty indirection module was created just to
      match the wording. SUITES: npm run test 63 files / 991 tests green, npm run
      build (tsc -b plus vite) clean, npm run lint exit 0 with the same five
      pre-existing warnings as main, dotnet build Ways.slnx clean. The three
      dotnet test suites were NOT run because this slice touches zero backend
      files ('git diff main --name-only' outside src/Ways.Web/ is empty). ZERO
      SCHEMA HELD: no migration file, has-pending-model-changes clean,
      InicializadorDeBaseDeDatos.cs / Politicas.cs / ManejadorDeErrores.cs
      untouched, zero physical deletes, zero DDL. PRE-EXISTING FLAKE OBSERVED AND
      NOT INTRODUCED HERE: Reposicion.test.tsx's first-punto-de-venta test fails
      intermittently under the full-suite run (1 of 3 runs on this branch, 1 of 4
      runs on main with the branch stashed; 12/12 when the file runs alone).
      TASK 2.17 ADDED MID-SLICE ON AN OWNER-REPORTED BUG, on the same branch
      feat/stage20-slice2-web-relaciones and web-only. As a platform actor,
      creating a user with rol Admin always failed with the backend 400
      tenant_requerido because the create form offered no tenant. The server
      already supported it (CrearUsuario accepts int? IdTenant,
      Contratos.cs:43-49) and the web mirror had dropped the field, which is a
      dto-contract-honesty violation on the web side. Delivered - tipos.ts
      mirrors idTenant on CrearUsuario and ActualizarUsuario is deliberately
      left alone because the server record does NOT accept a tenant
      (Contratos.cs:51-55); a tenant select in the create form rendered ONLY
      for a platform actor, so a tenant admin never enumerates tenants (S5) and
      sends idTenant null, which ServicioDeUsuarios.CrearAsync ignores in
      favour of Actor.IdTenant; the options derive from the ALREADY-LOADED rows
      via the existing opcionesDeTenant(filas) minus the platform token, so NO
      second fetch was added and D15 still holds; a client-side mirror of
      PoliticaDeRoles.ValidarConsistenciaDeRolYAlcance for guidance only (rol
      Root disables the selector and clears idTenant IN STATE, any other rol
      makes it required) with the server left untouched as the authority and
      its 400 still surfacing through the existing error path; and
      react-async-state rule 5 applied so the selector is inert while the list
      loads and while a save is in flight. FIVE MUTATIONS RUN FOR REAL
      (M14-M18, table in tasks.md), ALL KILLED, no survivors - dropping
      idTenant from the payload builder killed 3 tests, dropping the state
      clear killed the Root test on the rendered value, dropping the
      ofreceTenant gate killed 2, dropping the assignable-tenants filter killed
      the option-set test and dropping tenantsCargando killed the inert-while-
      loading test. ONE INACCURACY IN A TEST DOC-COMMENT was found by running
      M15 and corrected rather than left standing - the draft claimed the M4
      confound applied, but the select's value derives from the SAME state that
      travels in the POST so there is no independent fallback. TWO PREMISES OF
      THE LAUNCH BRIEF DID NOT HOLD AND ARE RECORDED, NOT ABSORBED - there is
      no tenant fetch in slice 2 to reuse (D15 forbids one) so the reused state
      is the derived opcionesDeTenant(filas), and Usuarios.tsx had no
      esPlataforma notion (Empresas.tsx:30 and PuntosVenta.tsx:41 do), which
      was introduced here with the identical rolId === ROL.Root definition.
      KNOWN GAP STATED - deriving from loaded rows means a platform actor can
      only assign a tenant present on the current page, and GET /api/usuarios
      defaults to tamanio 25; every tenant is provisioned with its admin user
      so a zero-user tenant does not normally exist, and the reachable case is
      the 26th tenant onward. Closing it needs either the platform-only
      listarTenants() fetch or the server-side pagination already deferred with
      a reopen condition, and that is an orchestrator decision, not an
      apply-phase one. SUITES after 2.17 - npm run test 63 files / 997 tests
      green, npm run build (tsc -b plus vite) clean, npm run lint exit 0 with
      the same five pre-existing warnings. Diff of the addition measured with
      'git diff --stat -- src' against the slice-2 tip - 3 files, 270
      insertions plus 4 deletions, of which Usuarios.test.tsx is 182. ZERO
      BACKEND FILE, zero schema, zero migration. PAGE-SIZE GAP CLOSED, narrow
      web-only follow-up on the same branch - the create selector now sources
      its options from clienteDeOrganizacion.listarTenants() (GET
      /plataforma/tenants, SoloPlataforma) gated strictly on esPlataforma, so
      a platform actor can assign any tenant regardless of the loaded users'
      page and a tenant admin never calls it (S5, M19 killed); the FILTER
      option set is untouched, still opcionesDeTenant(filas) with zero second
      fetch (D15). SUITES after the gap closure - npm run test 63 files / 999
      tests green, npm run build clean, npm run lint exit 0 with the same
      five pre-existing warnings. JUDGMENT-DAY SLICE 2 ROUND 1 CORRECTED, web
      only, no backend and no schema. Confirmed items fixed - C1 the tenant
      universe no longer catches to an empty list in silence (visible error plus
      a retry hung off opening "Nuevo"), C2 Guardar is inert for a platform
      actor while that universe is loading or failed (a disabled select is
      exempt from HTML constraint validation, so required alone let a POST with
      idTenant null through), C3 the tenant FILTER of Empresas and PuntosVenta
      is gated on esPlataforma exactly like the tenant COLUMN already was, C4
      tenants that are not Activo are still offered but their option label now
      carries the estado (the server never inspects the destination tenant's
      estado, so a user created inside a suspended tenant silently could not log
      in), S1 the unreachable non-platform arm of tenantsAsignables deleted,
      S2 the filter reconciliation written back to state instead of only derived
      at render, S3 the post-write refresh reloads with the APPLIED search term
      rather than the input draft, S4 the password POST split off the profile
      PUT so a committed write is never reported as a failure, S5 homonym owners
      disambiguated by id in the pure option helpers, S6 the rol-root branch
      downgraded to defence-in-depth (GET /roles cannot return Root for any
      actor) with the misleading comments rewritten, S7 ocupado cleared in a
      finally regardless of generation, S8 the repeated page-size rationale
      reduced to one, the Tenants invalidation doc-comment made truthful and the
      cuentaDePlataforma fixture given rol Root. TWO ITEMS DEFERRED BY THE
      ORCHESTRATOR AND RECORDED, NOT FIXED - the unpaginated 25-row window the
      usuarios filter operates on (named in task 2.12, code untouched, server
      pagination already deferred here) and the orphan option's id suffix
      (Reconciliacion 10, label deliberately unchanged). Twelve mutations run
      for real and KILLED (M20, M20b, M21, M22, M23, M24, M25, M26, M26b, M27,
      M28, M29); THREE HONEST SURVIVORS recorded as survivors, not dressed up -
      M21b the same-tick re-entrancy guard, M30 and M30b the ungated finally,
      all three unreachable today and kept as the trap slice 5's delete buttons
      will need. M17 is SUPERSEDED, not re-targeted - its clause was the dead
      arm S1 removed. SUITES after the correction - npm run test 63 files /
      1015 tests green, npm run build (tsc -b plus vite) clean, npm run lint
      exit 0 with the same five pre-existing warnings, measured against the
      pre-change baseline. JUDGMENT-DAY SLICE 2 ROUND 2 CORRECTED (FINAL
      round), web only, no backend and no schema. Confirmed by BOTH judges and
      fixed - R2-1 the tenant-universe failure now has its OWN rendered banner
      derived from tenantsDePlataformaFallo instead of sharing the error slot,
      which clobbered in both directions; R2-2 the password-POST failure moved
      to the red slot while the committed PUT keeps its green confirmation,
      both visible and the form still closes; R2-3 the S7 ungated finally
      replicated to Empresas and PuntosVenta for the POST-WRITE REFRESH only. NOT four-screen parity — the final re-judgment found the generation-mismatch returns inside the WRITE paths themselves (Usuarios.tsx:213/:220/:231, Tenants.tsx:88/:95, Empresas.tsx:79-84, PuntosVenta.tsx:107-113) still exit with ocupado latched; unreachable today by rule 9, carried to slice 5 as a binding input.
      Single-judge items fixed - R2-4 the "Nuevo" retry guarded on
      tenantsDePlataformaCargando, R2-5 the guardar() re-check announces itself
      instead of exiting silently, R2-6 the platform option included in
      desempatarHomonimos's collision map (keys untouched), R2-7 the usuarios
      tenant FILTER and COLUMN gated on esPlataforma for parity with C3, R2-8
      the PuntosVenta empresa write-back reconciled against the tenant-narrowed
      option set, R2-9 the Tenants invalidation rationale corrected (form and
      table are independent state), R2-10 the records - this Eleven-to-Twelve
      count, the blank line that severed M20-M30b from their table header, the
      stale supporting clause of Reconciliacion 10, and the note that the
      (baja) arm of etiquetaDeTenantAsignable is unreachable through the API
      today. ONE ITEM RECORDED WITH NO CODE CHANGE, BY DECISION - crafted
      tenant names that pre-embed a generated suffix are misleading copy only,
      the keys stay true and the text is platform-authored. Round 2 mutations -
      SEVEN KILLED (M31, M32, M33, M34, M34b, M36, M37) and THREE HONEST
      SURVIVORS recorded as survivors, not dressed up (M35 the M21b class, M38
      and M38b the M30/M30b class, all unreachable today). SUITES after the
      round-2 correction - npm run test 63 files / 1021 tests green, npm run
      build (tsc -b plus vite) clean, npm run lint exit 0 with the same five
      pre-existing warnings.

      SLICE 3 IMPLEMENTED (tasks 3.1-3.18 and the round-1 corrections 3.21-3.27
      checked; 3.19 judgment-day and 3.20 PR belong to the orchestrator). Branch
      feat/stage20-slice3-inspector-de-uso off main, measured with 'git diff main
      --stat -- src tests' and NOT estimated - 8 files, 1658 insertions and zero
      deletions, of which production is 585 across four files
      (Organizacion/InventarioDeDependientes.cs 335, Organizacion/InspectorDeUso.cs
      232, Abstracciones/IWaysDbContext.cs 11, DependencyInjection.cs 7) and tests
      are 1073 (Organizacion/InspectorDeUsoTests.cs 387,
      Persistencia/InventarioDeDependientesTests.cs 324, the new
      Ways.IntegrationTests/InspectorDeUsoEjecucionTests.cs 270 and the N3 golden
      Persistencia/Fixtures/inventario-de-dependientes.txt 92). THE GUARD SHIPS
      INERT AND THAT IS VERIFIED, NOT ASSUMED - 'rg -n
      "PrimeraDependenciaEnUsoAsync" src/' returns exactly one line, its own
      declaration; the only other src/ mention is the AddScoped registration, and a
      DI registration is not a call site. ZERO-SCHEMA HELD AND VERIFIED - zero files
      under Migraciones/, the last migration is still
      20260822002214_FiscalArcaEtapa19a, and 'dotnet ef migrations
      has-pending-model-changes' reports "No changes have been made to the model
      since the last migration."

      JUDGMENT-DAY SLICE 3 ROUND 1 CORRECTED. Round diff measured against the
      pre-round HEAD 31b9148 with 'git diff 31b9148 --stat -- src tests openspec' -
      7 files, 686 insertions and 49 deletions, of which production is 166 (31 in
      InspectorDeUso.cs, 135 in InventarioDeDependientes.cs). Six confirmed items
      fixed. C1 CRITICAL and confirmed by BOTH judges - puntos_venta was missing
      from the Tenant dependent set, because PuntoVentaConfiguration declares only
      HasOne<Empresa>() with the composite key (IdEmpresa, IdTenant) and no
      HasOne<Tenant>(), so Tenant.GetReferencingForeignKeys() never yielded it and a
      tenant whose customer opened a second local read PRISTINE - fail-OPEN, in the
      data-loss direction. Fixed by making the Tenant dependent set the UNION of the
      FK walk and every entity type assignable to EntidadTenant mapped to the
      id_tenant scope column, the same reflection idiom
      WaysDbContext.AplicarFiltroDeTenant already uses, deduplicated by (tabla,
      columnas) and classified by the same bucket rule; the fix closes the CLASS,
      not the instance. Adding the FK to the model was refused on sight - that is a
      schema change and reopens the gate. The N3 golden gained exactly one line,
      Tenant | puntos_venta | id_tenant | marcado, and nothing else, verified with
      'git diff --stat' on the fixture = 1 insertion. C2 - the raw-ADO execution
      half had ZERO tests ('rg PrimeraDependenciaEnUsoAsync tests/' returned
      nothing), so the bind order, the ramas.Any(UsaAncla) gate, the caller
      transaction and the ExecuteScalarAsync had no net; closed with the new
      Docker-backed InspectorDeUsoEjecucionTests over all four anchors including the
      two composite-key ones. C3 - the Marcado predicate deviated from design
      section A (column only, instead of EntidadBase AND the column), undeclared and
      in the under-blocking direction; both conditions are now required and N2
      recomputes with the same rule. C4 - the empty executable set returned null
      (fail-open) while Renderizar threw for the same state; PrimeraDependenciaEnUsoAsync
      now throws. C5 - the identifier pattern used ^...$, and in .NET $ also matches
      before a final newline, so "stock\n" passed; it is \A...\z now. C6 - a null
      element of valoresDeClave now raises an ArgumentException naming the index,
      and the carve-out rendering theory no longer asserts vacuous rows (Empresa
      added with an empty carve-out set).

      TWO DESIGN DEVIATIONS DECLARED, NOT SMUGGLED. The first is C3 itself - the
      implemented Marcado predicate was weaker than the design's, and it is now the
      design's. The second is N1 - the proposal asked for a completeness test and
      design section B answered with N1's branch-count equality, which is a
      TAUTOLOGY: it compared InventarioCompleto().Count against the very walk that
      produces it, so a dependent the walk cannot see is invisible on both sides. It
      is recorded as a tautology in tasks.md, N1's second half was rewritten to what
      it can honestly assert, and the new N5 is the set-level trip-wire N1 cannot
      be, because its universe comes from an independent source, the model's column
      mapping.

      TWO ITEMS RECORDED WITH NO CODE CHANGE, BY DECISION. R1 -
      InicializadorDeBaseDeDatos.cs:584 stamps the stage-2 backfill's
      listas_precio/clientes rows for PRE-EXISTING tenants with a later startup
      instant than those tenants' own created_at, so such a tenant is permanently
      blocked by clientes even with zero customer data; it is fail-SAFE
      (over-block), the discriminator is deliberately unchanged, and it is carried
      as a slice-4 input because the 409 names the blocking table so the operator
      sees clientes. R2 - ObtenerConexionAbiertaAsync opens without closing, which
      is byte-identical to the pre-existing idiom in AsignadorDeNumeroCliente,
      ServicioDeVentas, ServicioDeStock and ServicioDeLotes; recorded as a
      replicated pre-existing pattern, not a new defect.

      ROUND-1 MUTATIONS RUN FOR REAL, NEVER REASONED - SEVEN KILLED (M15 and M16 the
      union, killed by N5 naming puntos_venta and by the integration test getting
      null where it demanded "puntos_venta"; M19 the UsaAncla gate, killed 4/4 with
      Npgsql 08P01 "bind message supplies 2 parameters, but prepared statement ""
      requires 3"; M20 the bind order, killed 4/4 with Npgsql 42883 "operator does
      not exist: integer = timestamp with time zone"; M21 the fail-closed empty set;
      M22 the \z anchor, isolated from the message text so exactly the "stock\n"
      case died; M23 the positional null rejection) and TWO HONEST SURVIVORS
      recorded as survivors, not dressed up - M17 and M18, the two halves of the C3
      predicate, both SURVIVED 422/422 because on today's model the
      mechanical-impossibility throw already guarantees EntidadBase implies
      created_at and no type carries created_at without EntidadBase, so the three
      formulations are the same function and no test over this model can see the
      difference. They are equivalent mutants of the M3b class; the C3 fix is design
      conformance for the day a witness type appears, and N3's golden is what will
      name that day's table. M3b from the pre-round evidence is unchanged and still
      a survivor for the same reason.

      SUITES after the round-1 correction, run against real output and never
      inferred - 'dotnet build Ways.slnx' clean (0 errors, the two pre-existing
      NU1903 SSH.NET warnings), Ways.Domain.Tests 545/545 green, Ways.Application.Tests
      422/422 green (417 before this round; the five new are N5, the two new
      InspectorDeUso unit tests, the "stock\n" theory case and the Empresa theory
      row), Ways.IntegrationTests 1741/1741 green in 12 m 54 s run ONCE and ALONE
      against a single Docker daemon with zero transport-abort flakes and zero
      re-runs, and 'dotnet ef migrations has-pending-model-changes' clean.

      JUDGMENT-DAY SLICE 3 ROUND 2 CORRECTED (FINAL - no third round). Round diff
      measured against the pre-round HEAD 55fe4ac with 'git diff 55fe4ac --stat --
      src tests' and NOT estimated - 6 files, 521 insertions and 54 deletions, of
      which production is 263 (235 insertions plus 28 deletions) across the same
      two Application files (InventarioDeDependientes.cs 191/20, InspectorDeUso.cs
      44/8) and tests are 312. FIVE confirmed items fixed, ZERO schema, zero
      physical deletes, and the guard is still INERT.

      R2-1 CRITICAL, single judge but ORCHESTRATOR-VERIFIED - the Empresa anchor
      read PRISTINE with a full operating history, fail-OPEN, the same class as
      round 0's C1 by a different mechanism. NO operational table carries
      id_empresa - comprobantes_venta, comprobantes_compra, their items,
      pagos_comprobante, movimientos_stock, stock, stock_lotes, turnos_caja,
      movimientos_caja, movimientos_tesoreria, movimientos_cuenta_corriente,
      presupuestos, remitos, ordenes_compra and gastos all key on id_punto_venta -
      so an empresa's direct referencers were structure and catalogue only, the 13
      golden lines it already had. Provision, load tenant-wide articles (id_empresa
      NULL, so no articulos_empresas row), sell at the provisioned punto de venta,
      and all 13 branches fail: the 11 marcado ones need created_at > T0 and the
      provisioned rows are exactly T0, the 2 sinmarca ones have no row. Slice 4
      would have soft-deleted that empresa and its punto de venta. FIXED by making
      usage propagate UP the structural hierarchy - a punto de venta in use means
      its empresa is in use. InventarioDeDependientes gained a third source,
      AgregarRamasPuenteadasPorPuntoDeVenta: for the Empresa anchor every branch of
      the PuntoVenta anchor's EXECUTABLE inventory is re-emitted BRIDGED by
      puntos_venta, same buckets, same carve-outs, nothing hand-written. The new
      PuenteDeUso record carries the bridge table and its two column vectors, and
      the renderer emits FROM "public"."<hoja>" d JOIN "public"."puntos_venta" pv
      ON d."<fk>" = pv."id_punto_venta" AND d."id_tenant" = pv."id_tenant" WHERE
      pv."id_empresa" = $1 AND pv."id_tenant" = $2 [AND d."created_at" > $3]. ONE
      statement, not N queries per punto de venta; identifiers still come only from
      EF metadata and still pass the \A[a-z_][a-z0-9_]*\z validation; parameters
      stay positional and the bind order is unchanged, because
      PropiedadesDeAncla(Empresa) is still Id, IdTenant so the anchor instant is
      still $3. The label returned to the operator is the LEAF table, which is what
      the 409 has to name. Tenant does NOT need this - every table carries
      id_tenant and the scope-column source already brings it, verified by N5 on
      the Tenant anchor - and PuntoVenta and Usuario are leaves of the hierarchy.
      THE GOLDEN CHANGED BY EXACTLY 17 LINES AND NOTHING ELSE ('git diff --stat' on
      the fixture = 17 insertions, 0 deletions): the 17 executable branches of the
      PuntoVenta anchor, each justified table by table in tasks.md, rendered as
      'Empresa | <hoja> via puntos_venta | <columnas> | <balde>' - still four " | "
      separated fields, so N3's comparison needed no parser change, only the new
      via segment in the table field.

      R2-2 - N5 is now GENERIC over the four anchors instead of Tenant-only, and
      the round-1 claim that the union "closes the CLASS" was an overstatement, now
      corrected in tasks.md item 3.21: the union closes the class for EntidadTenant
      subclasses, while a tenant-scoped table that is NOT an EntidadTenant subclass
      is covered today by its declared FKs and by N5 as the CI trip-wire.
      N5_TodaTablaDeAlcanceDelAnclaApareceEnSuInventario is a theory over (ancla,
      columnas de alcance) - id_tenant; id_empresa; id_punto_venta +
      id_punto_venta_destino; id_empleado + id_empleado_apertura +
      id_empleado_cierre + id_actor - and every (tabla, columna) PAIR of each
      anchor's universe must appear in that anchor's inventory, with excluido lines
      counting as present and the failure NAMING the missing pair.

      R2-3 - three stale records corrected in place. M4 cited
      N1_LaCuentaDeRamasEsLaDeLasFksMenosLosCarveOuts, which round 1 deleted as a
      tautology, so the mutation was RE-RUN on this tree and the row now carries
      the observed messages. M10 recorded the pre-C5 ^...$ pattern and now says so.
      S1's InspectorDeUso.cs:72 is now re-observed per round (:72 originally, :77
      at 55fe4ac, :86 here) instead of copied forward. R2-4 - the two production
      doc-comments that narrated the fixed bug in past tense now state present
      intent; test-file comments keep their narrative. R2-5 - the synthesized
      tenant-scope branch zips one hardcoded column against the anchor's primary
      key, so a composite key would make Zip truncate in silence and bind only the
      first property; it now throws naming the key.

      ROUND-2 MUTATIONS RUN FOR REAL, NEVER REASONED - FOUR KILLED plus one re-run.
      M24a, the bridged family deleted from InventarioCompleto, killed 3
      Application tests with N3 printing all 17 QUITADAS lines. M24b, the same
      mutation observed through EXECUTION against real Postgres, killed 2/6
      integration tests with 'Assert.Equal() Failure: Strings differ / Expected:
      "turnos_caja" / Actual: null' - that failure IS the fail-open, the guard
      reporting an empresa with an open cash shift as pristine. M25, one FK-walk
      branch dropped for the Usuario anchor, killed N5(Usuario) which NAMED the
      exact pairs turnos_caja|id_empleado_apertura and turnos_caja|id_empleado_cierre;
      said honestly, N1(Usuario) and N3 also went red, because for the Usuario
      anchor every branch comes from the FK walk so no mutation of that walk can be
      invisible to N1 - N5's independence is what covers the anchors whose set is
      not only the walk. M26, a reachability row for the new arity guard, killed 6
      tests with the guard's own message; it proves the guard is reached and names
      the key, and it does NOT prove a composite Tenant PK would be caught, because
      no such witness exists in this model and none was fabricated - the same
      honesty class as M3b/M17/M18. M4 re-run killed 7 tests.

      SUITES after the round-2 correction, run against real output and never
      inferred - 'dotnet build Ways.slnx' clean (0 errors, the two pre-existing
      NU1903 SSH.NET warnings), Ways.Domain.Tests 545/545 green,
      Ways.Application.Tests 427/427 green (422 before this round; +3 from N5
      becoming a four-case theory and +2 from the two new bridge rendering tests),
      Ways.IntegrationTests 1743/1743 green in 12 m 37 s run ONCE and ALONE against
      a single Docker daemon with zero transport-abort flakes and zero re-runs
      (1741 before; +2 from the two new bridge execution tests), and 'dotnet ef
      migrations has-pending-model-changes' reports "No changes have been made to
      the model since the last migration." Slices 4-5 pending.

      SLICE 4 IMPLEMENTED (tasks 4.1-4.37 checked; 4.38 judgment-day and 4.39 PR
      belong to the orchestrator; 3.19 and 3.20 also checked now that PR #169
      merged in c3f62d7). Branch feat/stage20-slice4-bajas-api off main, measured
      with 'git diff main --stat -- src tests' and NOT estimated - 14 files, 2682
      insertions and 219 deletions, of which production is 681 insertions and 122
      deletions across 6 files (Organizacion/ServicioDeOrganizacion.cs 566 changed,
      Organizacion/EtiquetasDeTablas.cs 100 new, Usuarios/ServicioDeUsuarios.cs 54,
      Api/Endpoints/OrganizacionEndpoints.cs 33, plus the two comment amendments in
      InspectorDeUso.cs and InventarioDeDependientes.cs) and the rest is tests
      (BajasDeOrganizacionTests.cs 1552 new, BajasEstructuralesTests.cs 253 new,
      ProyeccionDeOrganizacionTests.cs 144, OrganizacionTests.cs 38,
      SuperficieDeAutorizacionTests.cs 9, and the constructor fixes of the three
      suites that build the two services by hand - 2001 insertions and 97 deletions
      in tests).
      THE SLICE OVERFLOWS THE 800-LINE BUDGET AND IT IS REPORTED, NOT HIDDEN -
      roughly 2150 authored lines against a forecast of 530, because the forecast
      predated three slice-1 judgment-day inputs that all landed here - the
      one-transaction re-projection of the four write paths, the six projection
      mutation kills, and the relocation's replacement assertions. The
      pre-authorized 4a/4b split is NOT clean as written (the relocation it puts in
      4b is forced by the transaction refactor it puts in 4a), a workable variant is
      recorded in tasks.md, and the decision is left to the orchestrator rather than
      taken unilaterally.

      ZERO SCHEMA HELD AND VERIFIED - zero files under Migraciones/, the last
      migration is still 20260822002214_FiscalArcaEtapa19a, 'dotnet ef migrations
      has-pending-model-changes' reports "No changes have been made to the model
      since the last migration.", and git status lists no file under
      src/Ways.Infrastructure/, no Politicas.cs and no ManejadorDeErrores.cs. The
      three DELETE routes reuse the policy of the group they already belong to.
      db-error-backstops STRUCTURALLY N/A - the deletion is an UPDATE ... SET
      deleted_at and DeleteBehavior.Restrict contributes exactly zero against it, so
      no SQLSTATE can fire and there is no branch to classify.

      ZERO PHYSICAL DELETES, ASSERTED AS CODE AND NOT AS A CLAIM -
      BajasEstructuralesTests scans every .cs under src/ - ExecuteDelete,
      ExecuteDeleteAsync, .Remove( and DELETE FROM are absent from the whole
      repository, and the six RemoveRange receivers are frozen by name, none of them
      an organization table. The trip-wire was proven by mutation (inserting
      db.Tenants.RemoveRange into EliminarTenantAsync turns it red).

      SIXTEEN MUTATIONS RUN FOR REAL AND ALL KILLED, NEVER REASONED - U1/U2/U3 the
      three cascade scopes, U4 the empresa cascade scope, U5/U6 the two minimum
      counts, U7a the FK conjunct (mutated to "(col = $n OR true)" so the parameter
      count is preserved and the failure is semantic, not a bind error), U7b the
      strict > against >=, U8 the SinMarca branch forced to carry the anchor
      conjunct (Postgres 42703, stock has no created_at), N4 provisioning reading
      the clock twice, and P1-P6 the six projection predicates that slice 1 shipped
      as SURVIVING mutants. Every one was applied alone and reverted byte for byte,
      with the harness asserting the file is identical afterwards. ONE MUTATION
      RECORDED AS NOT RUN INSTEAD OF DRESSED UP - the first U8 attempt (UsaAncla =>
      true) does not compile under CA1822 and was replaced by an equivalent one.

      THE FOUR SURVIVING MUTANTS OF SLICE 1 ARE DEAD, BY A DIFFERENT ROUTE THAN THE
      INPUT PREDICTED. The premise "the deletion writers are the reachable path" is
      false - the cascade takes a soft-deleted owner and its live children in the
      same instant, so no deletion writer can ever produce a live orphan. They are
      closed instead by re-routing below the confound, which is mutation-proof-tests'
      own instruction - the three projections are now public static and the tests
      compose the SAME production expression with IgnoreQueryFilters(["BajaLogica"]),
      which is exactly what no production caller does. The two contradictory
      doc-comments are reconciled into ONE rule stated at ProyeccionDeTenant (inside
      a projection every correlated subquery declares its own DeletedAt == null,
      because the expression is composable), the three counters were hardened
      accordingly, and the one predicate that could never be refuted -
      ServicioDeUsuarios.NombreDeTenantAsync, a query built and executed entirely
      inside its own method, where no caller can strip the filter - was REMOVED
      rather than carried a third round. Behaviour is unchanged on every path.

      THE FIRST-ARTICLE SCENARIO IS TRUE FOR TWO OF ITS THREE ENTITIES AND THE THIRD
      IS RECORDED, NOT PAPERED OVER (Reconciliacion 11 in tasks.md). No
      article-shaped row hangs off a punto de venta - articulos is tenant-wide and
      the per-empresa availability exception lives in articulos_empresas - so one
      article blocks the tenant and the empresa, and the punto de venta blocks on
      its own smallest customer datum, one stock row. It is the slice-3 design
      amendment in its own words, usage propagates UP the hierarchy and never down,
      and the spec text is left byte-identical, the same handling Reconciliacion 1
      gives OD6.

      TASK 4.10 RELOCATED SOMETHING ELSE THAN THE TASK PREDICTED. There were ZERO
      EliminarAsync cases to move - the organization service had no deletion at all
      and the usuario deletion was already covered by PreciosYUsuariosAuditoriaTests
      in the integration suite. What had to move is the consequence of the slice-1
      input - one transaction around each write and its re-projection makes the four
      write paths unrunnable on InMemory (TransactionIgnoredWarning). Five
      round-trip cases moved against their existing equivalents in OrganizacionTests
      and every one asserts what it asserted before; where the equivalent lacked a
      field assertion it was ADDED, and the one case with no equivalent
      (UnAdminEditaSuPropioPuntoDeVentaOk) was written. Nothing was weakened.

      OD5 HONOURED - no API-level test exists for empresa_en_uso or
      punto_venta_en_uso, both are proven below the API with a hand-seeded second
      empresa/punto de venta, and no creation endpoint was added. OD6 HONOURED - the
      cascade-deleted admin gets 401 credenciales_invalidas and the suspended
      tenant's user still gets 403 tenant_suspendido, two tests, two codes. OD4
      HONOURED - a soft-deleted article still blocks. THE BRIDGED 409 NO LONGER
      SHIPS A BARE LEAF LABEL, and it is resolved at the copy layer so
      InspectorDeUso's contract and its two test suites stay untouched -
      EtiquetasDeTablas.DescribirBloqueo names the bridge when every branch for that
      leaf is bridged and falls back to the bare label when the leaf also has a
      direct branch, because there the inspector cannot say which one matched. Task
      4.36's index check covers BOTH relations for a bridged branch and reports zero
      uncovered branches on this tree, frozen as an empty expected set so it is a
      trip-wire and not a report nobody reads. R1 (the InicializadorDeBaseDeDatos
      backfill over-block) was left exactly as it is, as instructed.

      SUITES OF SLICE 4, run against real output and never inferred - 'dotnet build
      Ways.slnx' clean (0 errors, the two pre-existing NU1903 SSH.NET warnings),
      Ways.Domain.Tests 545/545 green, Ways.Application.Tests 432/432 green (427
      before this slice - minus the five relocated round-trip cases, plus the ten of
      the new BajasEstructuralesTests), and Ways.IntegrationTests 1771/1771 green
      run ONCE and ALONE against a single Docker daemon in 12 m 48 s, with zero
      transport-abort flakes and zero re-runs, and confirmed by a SECOND full run of
      the same tree (1743 before - plus 26 of BajasDeOrganizacionTests,
      plus the projection-predicate kill and the relocated
      UnAdminEditaSuPropioPuntoDeVentaOk). THE FIRST FULL RUN WAS 1770/1771 AND THE
      FAILURE IS REPORTED, NOT SWALLOWED - SuperficieDeAutorizacionTests, the
      omission guard that walks the real EndpointDataSource, went red on the three
      new DELETE routes exactly as designed; the allowlist declares them with their
      reason (family (b) - an administrative surface stricter than
      GestionDeCatalogo, which never admits Vendedor) and the suite was re-run whole
      and green.

      JUDGMENT-DAY SLICE 4, RONDA 1 - SIETE HALLAZGOS CONFIRMADOS, LOS SIETE
      CORREGIDOS, CERO ESQUEMA. Lo primero fue VERIFICAR que auditoria.accion es una
      columna text libre cuyo unico CHECK es length(btrim(accion)) > 0
      (AuditoriaConfiguration.cs:22,36): agregar valores de accion es codigo y no
      esquema. Si hubiera estado restringida, C1 se habria DETENIDO y reportado en
      vez de tocarse. Politicas.cs, InicializadorDeBaseDeDatos.cs y
      ManejadorDeErrores.cs siguen sin aparecer en el diff, no hay migracion, DDL,
      sentencia de datos ni cambio de semilla, y no hay ningun borrado fisico nuevo.
      C1 (juez B, WARNING) - las tres bajas de organizacion no escribian NINGUNA fila
      de auditoria y la cascada del tenant daba de baja usuarios sin la usuario.baja
      que el camino directo siempre escribe: la accion mas destructiva del sistema no
      dejaba rastro en GET /api/auditoria. Se agregaron tres acciones al catalogo
      (tenant.baja/tenant, empresa.baja/empresa, pv.baja/punto_venta - pv abrevia el
      dominio igual que cc.reliquidacion, porque el formato congelado
      ^[a-z]+\.[a-z]+$ no admite guion bajo), dos fabricas de payload y cuatro call
      sites DENTRO de la misma transaccion; la cascada escribe una fila por entidad
      estampada y reusa PayloadDeAuditoria.BajaDeUsuario tal cual. El id_tenant es el
      del SUJETO, asi que plataforma dando de baja el tenant X deja el rastro EN el
      tenant X, donde su admin lo lee.
      C2 (juez A, WARNING) - ServicioDeUsuarios.EliminarAsync corria el guard como un
      SELECT suelto fuera de toda transaccion y estampaba deleted_at despues: una
      venta o un turno del MISMO empleado en el medio eran invisibles. Guard +
      estampado + auditoria quedaron en UNA transaccion dentro de
      CreateExecutionStrategy().ExecuteAsync, bajo el MISMO
      pg_advisory_xact_lock(idTenant, -20) de las bajas de organizacion; el sujeto de
      plataforma (IdTenant null) bloquea sobre el centinela 0, documentado.
      PoliticaDeRoles queda afuera a proposito.
      C3 (juez B, WARNING) - DescribirBloqueo degradaba a la etiqueta pelada cuando la
      hoja tenia ademas una rama directa; ahora alcanza con que ALGUNA rama sea
      puenteada. C4 (juez A) - cuarto walker de autorizacion que afirma la policy
      exacta de DELETE /api/puntos-venta/{id:int}, cuyo grupo NO tiene policy propia.
      C5 (juez A) - el ancla del guard de usuarios se resuelve POR NOMBRE contra
      InventarioDeDependientes.PropiedadesDeAncla. C6 (ambos jueces) - el trip-wire de
      borrado fisico se anclo (db.<Set>.Remove( con receptor congelado, DELETE FROM
      acotado a lo que no es migracion) para que no se ponga rojo por un List.Remove
      de la BCL. C7 (juez B) - arqueos_turno etiquetado "arqueos de caja", el termino
      que ya usan CierreDeCaja/CajaZ.
      SEIS MUTACIONES CORRIDAS DE VERDAD Y TODAS MATADAS, MAS UNA REGISTRADA COMO SIN
      KILL - C1 (sacar RegistrarBajaDeTenant), C2 (mover el guard afuera de la
      estrategia, ESTRUCTURAL y dicho como estructural), C3 (restaurar TrueForAll), C4
      (sacar el RequireAuthorization del DELETE, y solo el walker nuevo se puso rojo,
      confirmando que los otros tres eran ciegos), C6 en sus DOS brazos
      (db.Tenants.Remove y un DELETE FROM en archivo de produccion) y C7 (sacar la
      entrada del diccionario). C5 se registra HONESTAMENTE como sin kill
      independiente: el conjunto de anclas de Usuario es ["Id"] hoy, asi que la
      resolucion por nombre y la posicional son observacionalmente identicas hasta el
      dia en que Usuario tenga clave compuesta - que es exactamente el dia en que el
      hardcode se habria roto en silencio.
      DOS PRUEBAS PREEXISTENTES SE ROMPIERON POR EL FIX Y SE ARREGLARON SIN
      DEBILITARLAS - SoloFilasDeAuditoriaNoBloqueanLaBajaYElRastroSigueResolviendo
      pasaba a ver dos filas sobre el mismo id_entidad (la sembrada mas la usuario.baja
      de la cascada) y ahora nombra su accion; y el nuevo assert de instante comparaba
      el payload en memoria (100 ns) contra la columna timestamptz (microsegundos), asi
      que se trunca a microsegundos - una diferencia de representacion, no de instante.
      TRES ITEMS REGISTRADOS SIN CODIGO - R1 sigue ACEPTADO y ahora cubre tambien la
      baja de usuario (el lock serializa baja contra baja, no baja contra venta; el
      modo de falla es un punto de venta o un usuario dado de baja con una operacion
      posterior, recuperable a mano porque B1 deja todas las filas); la cascada solo es
      alcanzable sobre hijos creados en el instante del ancla, por diseno; y la
      Reconciliacion 11 quedo registrada y sana.
      JUDGMENT-DAY SLICE 4 RONDA 2 (FINAL, sin tercera) CORRIGIO DIEZ ITEMS CONFIRMADOS.
      Cero schema otra vez: ni migracion, ni DDL, ni seed; Politicas.cs,
      InicializadorDeBaseDeDatos.cs y ManejadorDeErrores.cs no aparecen en el diff.
      R2-1 (CRITICAL del juez B) se corrigio con la OPCION (b) y esta dicho por que:
      las cuatro bajas corren bajo FabricaDeEstrategiaSinReintento -el mecanismo que
      el repo ya tiene para operaciones raras, humanas y sin clave de idempotencia,
      el mismo de AnularAsync y AjustarAsync- en vez de un ChangeTracker.Clear() que
      no tiene ningun precedente aca, que instalaria un invariante nuevo que cualquier
      edicion futura rompe en silencio, y que ademas no arregla el commit ambiguo (el
      reintento releeria la fila ya dada de baja y contestaria 404 a una baja que si
      tuvo exito). El costo se dice: una falla transitoria sobre una baja ahora llega
      al operador en vez de absorberse. La mutacion reprodujo el hallazgo entero:
      intentos=2, filas=8, y la segunda tenant.baja afirmando anterior estado "baja".
      R2-2 agrego la relectura del sujeto bajo el lock en ServicioDeUsuarios, matada
      POR DEBAJO del pre-chequeo con un DbTransactionInterceptor que corre la cascada
      del tenant al abrirse la transaccion (el lock es ADO crudo y no lo ve ningun
      DbCommandInterceptor). R2-6 se arreglo EN LA RAIZ: el inspector proyecta la
      etiqueta de la RAMA (hoja via puente) compuesta de identificadores ya validados,
      y DescribirBloqueo la parsea en vez de adivinar del conjunto de ramas; las dos
      redacciones anteriores se equivocaban sobre la misma hoja mixta (parametros).
      R2-5 ensancho el barrido de borrado fisico a tres anclajes con receptor
      congelado y volvio a incluir migraciones en DELETE FROM; correrlo DESCUBRIO UN
      MUTANTE SOBREVIVIENTE (Set<T>().Remove sin receptor escapaba a \.Set<...>) que
      se cerro ensanchando el ancla a \bSet<...>. R2-3 subio el catalogo del front a
      15 con su test de congelamiento; R2-4 sumo el PUT de puntos-venta al walker de
      policy por ruta; R2-7 corrigio un comentario falso (el rastro del tenant NO es
      legible por GET /api/auditoria hoy) y quedo como item diferido con su condicion
      de reapertura; R2-8 agrego por_cascada a la usuario.baja de la cascada; R2-9
      sumo los dos 409 estructurales y una falla NO transitoria con el rastro ya
      encolado. R2-10: la clase era mas ancha que los dos archivos nombrados - SEIS
      archivos de esta rama tenian BOM introducido aca (git show main:<path> lo
      prueba) y se sacaron los seis. DOCE MUTACIONES CORRIDAS DE VERDAD, cada una con
      su mensaje real, once matadas y una registrada como sin kill independiente (la
      atomicidad de la transaccion no es expresable como mutacion de una clausula
      porque el lock la exige estructuralmente). SUITES: dotnet build limpio (2
      NU1903 preexistentes), Domain 545/545, Application 434/434, Integration
      1780/1780 en 12m49s corrida UNA sola vez y SOLA contra el daemon de Docker,
      cero flakes y cero re-runs; web 63 archivos / 1022 tests, build y lint limpios
      (5 warnings preexistentes de react-refresh); has-pending-model-changes limpio.
      Slice 5 implemented; judgment-day and PR pending.
      SLICE 5 IMPLEMENTED (tasks 5.1-5.10 checked, plus 4.38/4.39 closed by PR 171
      / commit 99b5abe; 5.11 judgment-day and 5.12 PR belong to the orchestrator).
      Branch feat/stage20-slice5-bajas-web off main, web only. MEASURED with 'git
      diff main --numstat' and NOT estimated - 13 files, 2035 insertions and 192
      deletions, of which production is 901 (722 insertions, 179 deletions) and 745
      ignoring pure re-indentation, tests are 1240 and docs are 86. THE ESTIMATE OF
      ~340 IS OVERRUN BY ROUGHLY 6x AND THAT IS REPORTED, NOT HIDDEN - three causes,
      all of them structural rather than scope creep: (a) mutation-proof-tests and
      web-descriptor-tests demand a test per branch, and rule 10 replicates every
      pattern across FOUR sibling screens, so 1240 test lines land against a forecast
      that budgeted for one screen's worth (slice 2 hit the same shape: 1078 test
      lines against ~430); (b) the four carry-forward items of slice 2 required
      restructuring EVERY write path of the four screens onto a single ungated
      finally, which is a whole-file re-indentation on top of the semantic change;
      (c) a real defect was found while running the mutations and its fix is a fifth
      cross-cutting change (below). THE NATURAL SPLIT, if the orchestrator wants one,
      is already cut into the eight commits: 5a = organizacion.ts + bajas.ts +
      ConfirmacionDeBaja + Tenants + Empresas, 5b = PuntosVenta + Usuarios + docs.
      A REAL DEFECT FOUND BY A TEST, NOT REASONED ABOUT - the re-entrancy guard of
      react-async-state rule 9 was written as 'if (ocupado) return' reading STATE,
      and two clicks of the SAME tick read the same render, so both passed and two
      DELETEs went out. The slice-5 re-entrancy test failed on the first run and the
      fix is a synchronous ref mirror (ocupadoRef), replicated to all four screens in
      the same PR. This is exactly the M35/M21b survivor slice 2 recorded as
      unreachable: the delete buttons made it reachable and it now has a real kill.
      THIRTEEN MUTATIONS RUN FOR REAL (MS1-MS12 plus MS7b, table in tasks.md): ten
      killed with their exact failing test names, TWO HONEST SURVIVORS recorded as
      survivors and not dressed up (MS1 the post-write generation check and MS3 the
      ungated finally - both the 'unreachable while rule 9 holds' class carried from
      slice 2, and the honest result of RUNNING them is that the delete buttons did
      NOT make them reachable, because the gate is itself disabled during an
      outstanding write), and ONE DISCARDED as a badly-formed mutant (MS7 was a
      no-op: confirmarBaja reads baja from the render closure; replaced by MS7b,
      which kills). THE FOUR CARRY-FORWARD ITEMS OF SLICE 2 ARE CLOSED - (1) the
      generation-mismatch returns inside the write paths no longer latch ocupado:
      every write of the four screens now has ONE ungated finally covering all its
      exits, and refrescarTrasEscribir stopped owning the flag; (2)
      ERROR_ALTA_SIN_TENANTS has its own slot, and buscar() deliberately does NOT
      clear it because it reports a LIVE precondition of the open form, while
      errorPassword is cleared on Cancelar and Buscar because it reports a finished
      write; (3) the platform option still counts for the collision but never
      receives the suffix, so the sin-tenant sentinel cannot reach the copy; (4) the
      tenant-universe banner is gated on esPlataforma and the tenanteReconciliado
      typo is fixed. ANTI-ORACLE AT THE UI LAYER - a 404 renders a neutral
      'ya no existe o no esta a tu alcance' with the server message deliberately NOT
      appended, so a future server text cannot leak through; asserted per screen.
      THE 500 COPY carries 'verifica el listado antes de reintentar' per the slice-4
      carry-forward, because the bajas run without automatic retry and a lost ACK
      means the deletion may have committed. V1-V6 RE-ASSERTED ON THE WHOLE STAGE
      DIFF (22af91a..HEAD): zero migration files touched by the entire stage, the
      last migration is still 20260822002214_FiscalArcaEtapa19a.cs,
      InicializadorDeBaseDeDatos.cs / Politicas.cs / ManejadorDeErrores.cs untouched,
      zero physical deletes over the four organization tables (the only RemoveRange
      hit is the pre-existing articulos_empresas replace-set, another table and a
      known carryover), zero ': IWaysDbContext' implementers. SUITES - npm run test
      64 files / 1071 tests green, npm run build (tsc -b plus vite) clean, npm run
      lint exit 0 with the same five pre-existing react-refresh warnings, dotnet
      build Ways.slnx clean with the 2 pre-existing NU1903 advisories. FLAKE NOTE,
      recorded rather than swallowed - the first full-suite run reported Remito and
      one Usuarios selectOptions as failures; both pass in isolation and the very
      next full run was 1071/1071 green. Same non-reproducible parallel-load class
      already recorded for this repo; nothing in this slice touches Remito. TWO
      FILES OUTSIDE src/Ways.Web ARE TOUCHED ON PURPOSE - docs/09-multi-tenancy.md
      and docs/10-modelo-de-datos.md, which task 5.9 requires. Zero backend and zero
      schema, as the constraint intends. DELIVERY REPORT OWED TO THE OWNER (OD5,
      task 5.12) is in the apply return envelope: empresa and punto de venta
      deletion ships LATENT, plus the two deferred items R1 and T6.
  verify:
    status: pending
    artifact: verify-report.md
    depends_on: [apply]
  archive:
    status: pending
    artifact: archive-report.md
    depends_on: [verify]
dependencies:
  stages:
    - stage-1-organization-and-catalogs (archived) — the organization hierarchy,
      EntidadBase/DeletedAt, the "BajaLogica" query filter, the estado_tenant enum
      already carrying 'baja', and RLS.
  code:
    - ServicioDeAprovisionamiento (ADR-16) — the SINGLE clock reading at :46 that
      stamps one identical instant on every provisioned row including the tenant
      itself; the entire pristine/used discriminator rests on this property.
    - PoliticaDeRoles — consumed UNCHANGED by decision 5 (the usage guard is
      inserted after ValidarPuedeIntervenirSobre, never instead of it).
    - WaysDbContext.AplicarFiltroDeBajaLogica / AplicarFiltroDeTenantEnTenant —
      the existing metadata-walk idiom the guard follows.
    - IRelojDelSistema / RelojFijo — the shared deletion instant and its tests.
  platform:
    - EF Core IEntityType.GetReferencingForeignKeys() (.NET 10) — no new package.
    - NO new NuGet package, NO new web dependency, NO migration, NO external service.
  skills:
    - mutation-proof-tests, dto-contract-honesty, web-descriptor-tests,
      work-unit-commits, judgment-day (before every PR).
    - db-error-backstops is EXPLICITLY N/A for this change — see the note below.
  blocked_on: >-
    Nothing external. The only gate is the owner's ratification of the
    ZERO-SCHEMA db_gate before slice 1.
notes:
  binding_owner_decisions:
    B1: >-
      PHYSICAL DELETION IS NEVER PERMITTED. All deletion stays logical
      (DeletedAt), consistent with the EntidadBase convention. Enforced as a
      verify criterion: a repository scan for ExecuteDelete, Remove(,
      RemoveRange( and 'DELETE FROM' over tenants, empresas, puntos_venta and
      usuarios. Direct dividend: NOTHING in this stage is unrecoverable, so
      rollback is git revert plus at most a one-line UPDATE.
    B2: >-
      "IN USE" MEANS THE CUSTOMER LOADED OR OPERATED ANYTHING BEYOND THE
      PROVISIONING BASELINE — articles, clients, suppliers, a second punto de
      venta, not only transactional records. Only what
      ServicioDeAprovisionamiento created counts as pristine. A tenant with
      3000 articles and zero sales is NOT deletable. This replaced an earlier,
      narrower reading during explore.
    B3: >-
      THE DISCRIMINATOR IS dependent.CreatedAt > entity.CreatedAt, valid
      because ServicioDeAprovisionamiento.cs:46 reads the clock ONCE
      ('var ahora = reloj.Ahora;') and stamps that identical instant on every
      provisioned row including the Tenant itself (:53-54, 69-70, 79-80,
      89-90, 104-105, 121-122, 142-143, 158-160). Template-version
      independent. GUARDED FROM A DISTANCE by a regression test asserting a
      freshly provisioned tenant is pristine — it fails the moment a refactor
      breaks the single-reading property.
    B4: >-
      THE DEPENDENT SET IS DISCOVERED FROM EF METADATA
      (IEntityType.GetReferencingForeignKeys()), NEVER a hand-maintained table
      list. A hand list fails UNSAFE (one forgotten table silently makes a
      used entity deletable); metadata fails SAFE. Decision 4 makes the
      fail-safe claim VERIFIABLE via the completeness test, because a
      fail-safe property that is never detected is only a hope.
    B5: >-
      AUDIT ROWS DO NOT BLOCK DELETION. Auditoria.IdActor and
      Auditoria.IdPuntoVenta are a trail ABOUT the entity, not data the
      customer operated, and because deletion is logical the referenced row
      survives so the trail keeps rendering. Implemented as ONE of exactly
      TWO named carve-outs (the other is NumeracionCliente, the provisioning
      counter row), each with a written reason in code and its own test.
  db_error_backstops_not_applicable: >-
    The db-error-backstops skill's SQLSTATE backstop DOES NOT APPLY to this
    change. Every FK in WaysDbContext is DeleteBehavior.Restrict, but B1 means
    no physical DELETE ever runs, so no Postgres constraint can EVER fire
    against an UPDATE ... SET deleted_at. The application guard is the SOLE
    line of defence and its tests carry the entire burden. Recorded as the
    stage's top risk; it is why decision 4's completeness test is listed among
    the four items that may never be degraded.
  never_degrade:
    - The completeness test (every referencing FK classified into exactly one
      of the three buckets; an unclassified type is a RED TEST naming it).
    - The two carve-out tests (auditoria-only and numeraciones_clientes-only
      entities remain deletable).
    - The "one article blocks" test — B2 proven end to end, not asserted.
    - The zero-physical-delete repository scan.
  deferred_with_reopen_conditions:
    - Force/override delete for a used entity — suspension is the existing
      lever; an override needs an audit ceremony this stage would design
      blind. REOPEN — the first real support case that needs it.
    - Undelete/restore endpoint — rows are recoverable by hand and
      EstadoTenant.Baja is deliberately unreachable from suspend/reactivate.
      REOPEN — its own change, with its own guard question.
    - Retro-guarding the other unguarded soft deletes (ServicioDeCatalogo<T>,
      Clientes, Proveedores, Articulos, Ofertas). InspectorDeUso is designed
      entity-agnostic so adoption is a one-line change. REOPEN — a later stage.
    - Estado/suspension for empresa and punto de venta. REOPEN — explicit owner
      request.
    - Server-side pagination/filtering for the four root lists (decision 7 —
      when the client-side filter stops being viable, the unbounded list is
      what broke, and all four screens move together). REOPEN — list size.
    - A minimum-administrators-per-tenant invariant
      (ultimo_admin_del_tenant) — flagged in the proposal question round
      rather than invented autonomously. REOPEN — owner answer.
    - A platform-readable audit surface. The tenant.baja row persists in
      tenant X for forensics and export and is readable at the database, but
      GET /api/auditoria is Admin-only (Politicas.LecturaDeAuditoria) and the
      cascade has just deleted every admin of that tenant, so nobody reads it
      through the API today. Politicas.cs stays untouched by decision
      (criterion V5). REOPEN — the first time platform needs a deleted
      tenant's trail without going to the database (judgment-day slice 4,
      ronda 2, R2-7).
  known_trap_from_explore: >-
    The structured 'phases:' block of this file must be kept ACCURATE by every
    later phase — stage-18's state.yaml left it stale while the real progress
    lived in a trailing comment block. Each phase updates its own entry
    (status, date, artifact, notes) as part of its work, and the trailing
    free-text log is NOT an acceptable substitute.
